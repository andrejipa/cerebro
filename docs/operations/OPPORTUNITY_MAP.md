# Opportunity Map

## Current Snapshot — 2026-05-23

- Publication follow-up opened on 2026-05-23: PR #1 is merged into `main` as `71d8be3f`, local `main` is aligned, and the official Windows base gate is green. Before this operator request, the queue had no eligible work because `observation-center-sqlite-ledger` was blocked. The current request is the formal trigger `FORMAL_RESUME_TRIGGER_OBSERVATION_CENTER_SQLITE_LEDGER_2026-05-23`, authorizing the five scoped follow-up points: docs reconciliation, blocked/stale UX clarity, separate MCP CI validation, operational navigation, and controlled SQLite observation-center promotion.

- Phase 11 (agent-agnostic bootstrap) complete on 2026-05-09: walk-up root detection, cerebro init scaffold (AGENTS.md universal, never CLAUDE.md), --repair-scaffold idempotent, --project-root sovereign, 28 new tests. Any LLM can operate cerebro. Next: observation-center-sqlite-ledger (open under the 2026-05-23 formal trigger).

- Runtime Manager Phases 7–10 complete on 2026-05-09 (schema v15 unchanged before this round, gate 2418/0/6). Ph7: STDIO MCP server 12 tools, PersistentRateLimiter, eval_mcp_safety.py. Ph8: classify_action() L0–L4, schema v15, L4 blocked, CLI policy classify/explain-levels, eval_autonomy_levels.py. Ph9: per-op token re-auth _require_current_token_factory, RuntimeManagerStoreError.code, BEGIN IMMEDIATE rate limit, 19 cross-process tests, THREAT_MODEL T16–T19. Ph10 (local hardening): experiments/runtime_manager_stress_lab/ 8 scenarios (stress_pass_is_not_permission=True); RuntimeIntegrityReport + check_integrity() + CLI integrity check; removed _max_level dead code; mcp_level_blocked counter in factory for all tools; stable error codes (autonomy_level_blocked, rate_limited, replay_path_out_of_scope); adapters in pyproject.toml; 8 arch tests; eval_phase10_hardening.py 8 evals. T20–T22 remain open for a future external-transport phase, not this local SQLite promotion.

- Control-plane loop-stop and cross-review consistency fronts advanced on 2026-05-08. Both remain advisory only.

- Control-plane adversarial posture front advanced on 2026-05-08. Advisory only.

- Runtime Manager Phases 4+5+6 complete on 2026-05-08 (schema v13, gate 2175/0/6). All phases archived.
- Runtime Manager event vocabulary delivered on 2026-05-08: `runtime_opened` now fires exactly once before any `queue_item_observed` on first sync; `runtime_synced` payload enriched with all import counts; `read_status`/`read_next` proven event-isolated; stale live-center tests migrated to synthetic helpers. Contract updated. Full gate: `1887/0/0/6`. No executor, adapter, MCP, Temporal, LangGraph, Agents SDK, cloud, target mutation, or free command.
- Runtime Manager diagnostic severity delivered on 2026-05-08: `RuntimeGateDiagnostic` now exposes machine-readable `severity` and `blocking` fields through core, CLI JSON, and CLI text. Blocking diagnostics such as `missing_validations`/`active_stop_condition` are now distinguishable from informational diagnostics such as `active_lease_expired`/`failed_replay_run`, so consumers do not have to infer readiness from diagnostic names. This builds on the replay diagnostics hardening (`replay_runs_passed`/`replay_runs_failed`, `failed_replay_run`, schema guards, read-only event isolation). Full AGENTS-equivalent gate: `1012/0/0/6`. No executor, adapter, MCP, Temporal, LangGraph, Agents SDK, cloud, target mutation, or free command.
- Runtime Manager lease expiry/readiness semantics delivered on 2026-05-08: expired active leases (`status="active"` with parseable past `expires_at`) no longer block single-flight selection. New `leases_expired` counter, `active_lease_expired` gate diagnostic (informational, visible even on success), and conservative treatment of empty/malformed `expires_at` as still-active. `RUNTIME_MANAGER_CONTRACT.md` now has a Lease Expiry Contract section. Gate passed `1008/0/0/6`. No executor, adapter, MCP, Temporal, LangGraph, Agents SDK, cloud, target mutation, or free command.
- Control-plane runtime-management review front advanced on 2026-05-08: `experiments/control_plane_runtime_state_review/`, `experiments/control_plane_runtime_contract_review/`, `experiments/control_plane_runtime_state_transition_review/`, `experiments/control_plane_tool_manifest_review/`, `experiments/control_plane_evidence_policy_review`, `experiments/control_plane_work_queue_review/`, and `experiments/control_plane_approval_policy_review/` are closed advisory slices. The live Runtime Manager bridge now has schema v6 validation/stop-condition tables, structured `gate_diagnostics`, and `selection_audit` with policy version, sort policy, ordered eligible ids, candidate entries, sort keys, and blockers.
- Control-plane decision-version review slice closed on 2026-05-08: `experiments/control_plane_decision_version_review/` now reviews caller-supplied decision records before any durable decision store exists. It catches revision gaps, duplicate/non-latest current decisions, invalid supersession, expired current decisions, missing human-decision ids, `auto_continue`, authority wording, stale handoff references, approvals over handoff/transition drift, unresolved action-review human decisions, file-read/decision-text laundering, and forged summaries. Focused validation passed: decision-version review `13/0`; boundary audit `12/0`; lifecycle `18/0`.

- Control-plane observation-set review slice closed on 2026-05-08: `experiments/control_plane_observation_set_review/` now evaluates caller-supplied observation-center snapshots plus optional action-review bundles for queue-contract coherence, single-flight drift, live resolved items, auto-continuation, dependency/trigger state, bundle/snapshot identity, focus drift, and multiple advisory bundles. Hardening rejects partial payload drift, authority-text laundering, bundle guardrail drift, and forged summary counts. At slice time, the center was coherent blocked evidence: `third-party-pilot-cycle-1` was waiting with unsatisfied dependencies and no open-ready frontier. Focused validation passed: observation-set review `14/0`.
- Control-plane integrity/action-review slices closed on 2026-05-08: `experiments/control_plane_integrity_review/` consolidates boundary-audit, guardrail-eval, and lineage-invariant reports into one advisory integrity status; `experiments/control_plane_action_review/` composes one caller-supplied observation-center item through assessment, capability, packet, matrix, telemetry, guardrail, lineage, and integrity evidence. The original `third-party-pilot-cycle-1` fixture stayed `waiting_checkpoint_blocked`; machine-primary status beats Markdown-like next-action wording. These are not runtime gates, truth signals, permission layers, CLI/adapters, schedulers, or target mutation. Focused validation passed: integrity/action review `14/0`; boundary/lifecycle `27/0`.
- Control-plane action-review hardening closed on 2026-05-08: `experiments/control_plane_action_review/` now fails closed on unknown capability decisions and capability guardrail drift, blocks `auto_continuation=true`, treats integrity drift as blocking posture, carries unsatisfied dependencies as missing evidence for any observation kind, and rejects bundle renderers that mix matrix/telemetry from a different packet. `experiments/control_plane_integrity_review/` now rejects summary laundering across boundary, guardrail, and lineage reports. Focused validation passed: action review `14/0`; integrity review `6/0`.
- Control-plane boundary-audit and lineage-invariant slices plus hardening closed on 2026-05-08: `experiments/control_plane_boundary_audit/` audits package boundaries recursively with local-only negative-marker suppression and direct `cli`/`extensions` import detection; `experiments/control_plane_lineage_invariant_eval/` checks whether meaning survives packet/matrix/lab/adversarial -> telemetry and telemetry -> guardrail boundaries. Both remain advisory only. Focused validation passed: boundary audit `9/0`; lineage invariant eval `16/0`.
- Control-plane guardrail-eval slice closed on 2026-05-08: `experiments/control_plane_guardrail_eval/` now evaluates `ControlPlaneTelemetryProjection` outputs for authority, permission, truth, and stability laundering. It catches approval/permission/execution/truth/stable-semconv vocabulary, exported telemetry markers, missing span/event non-permission guardrails, sensitive GenAI attributes, and packet verdict/status contradictions. It is not a runtime gate, truth signal, permission layer, CLI/adapter, exporter, scheduler, or target mutation. Focused validation passed: guardrail eval `8/0`; lifecycle registration was added and rerun.
- Control-plane telemetry-projection slice closed on 2026-05-08: `experiments/control_plane_telemetry_projection/` now projects packets, matrices, scenario-lab reports, and adversarial reports into deterministic in-memory INTERNAL spans/events with development-only OTel GenAI compatibility markers. It is not an OpenTelemetry exporter, stable semconv claim, permission layer, truth signal, CLI/adapter, runtime gate, scheduler, or target mutation. Focused validation passed: telemetry projection `9/0`; lifecycle registration was added and rerun.
- Control-plane scenario-lab slice closed on 2026-05-08: `experiments/control_plane_scenario_lab/` now runs declared in-memory scenarios through the review-packet and review-matrix chain, with a built-in scenario battery for clean advisory, missing capability, runtime/canonical blocked, and network-review-required paths plus adversarial probes for replay drift, packet/matrix guardrail drift, advisory-allow non-permission preservation, semantic laundering, and capability identity collisions. It reports expectation drift and adversarial findings, but neither expectation match nor findings are permission, execution approval, readiness, scheduler output, runtime authority, CLI, adapter, OpenTelemetry export, or target mutation. Focused validation passed: scenario lab `9/0`; lifecycle registration was added and rerun.
- Control-plane review-matrix slice closed on 2026-05-08: `experiments/control_plane_review_matrix/` now aggregates already-built review packets into observed counts for packet verdicts, review statuses, replay verdicts, replay statuses, blockers, replay issues, and required human decisions. It intentionally emits no single pass/fail matrix verdict and does not rank, choose, schedule, reconcile queue state, grant permission, approve execution, expose CLI/adapters, export telemetry, or become a runtime gate. Focused validation passed: review matrix `6/0`; lifecycle registration was added and rerun.
- Control-plane review-packet slice closed on 2026-05-08: `experiments/control_plane_review_packet/` now compresses the current assessment, capability, trace, JSONL ledger, and replay-eval chain into one operator-facing advisory packet. It returns packet verdicts for advisory review, human review, blocked review, and replay-invalid review, but explicitly says packet pass is not truth, permission, execution approval, readiness, runtime gate, scheduler, executor, CLI, adapter, or OpenTelemetry export. Focused validation passed: review packet `7/0`; lifecycle registration was added and rerun.
- Control-plane replay-eval slice closed on 2026-05-08: `experiments/control_plane_replay_eval/` now evaluates caller-supplied Control Plane JSONL replay artifacts and classifies them as contract-passed, incomplete, contract-failed, or authority-drift. It is an advisory replay-quality check only: pass is not truth, freshness, approval, permission, readiness, runtime gate, executor, CLI, adapter, OpenTelemetry export, or scheduler. Focused validation passed: replay eval `9/0`; lifecycle registration was added and rerun.
- Control-plane event-ledger slice closed on 2026-05-08: `experiments/control_plane_event_ledger/` now turns `ControlPlaneTrace` events into an in-memory JSONL replay artifact and parses it back through strict verification. It checks sequence continuity, one trace id, one replay digest, event vocabulary, per-row event digests, digest-is-not-truth guardrails, non-authority markers, and `decision_opened` / `decision_closed` boundaries. It is JSONL replay evidence only, not a canonical event log, writer, scheduler, permission layer, executor, CLI, adapter, OpenTelemetry exporter, or runtime gate. Focused validation passed: event ledger `8/0`; lifecycle registration was added and rerun.
- Control-plane trace slice closed on 2026-05-08: `experiments/control_plane_trace/` now correlates assessment and capability-policy inputs into a replayable advisory trace. It emits `combined_review_status`, human review recommendation, blockers, capability review ids, deterministic replay digest, and local trace events while rejecting non-advisory inputs. It is correlation/replay evidence only, not a scheduler, permission layer, executor, CLI, adapter, or runtime gate. Focused validation passed: control-plane trace `9/0`; lifecycle registration was added and rerun.
- Capability-policy slice closed on 2026-05-08: `experiments/capability_policy/` is now the Control Plane's first advisory tool-governance artifact. It loads TOML capability rules and evaluates proposed command/tool requests for argv prefix, path scope, data sensitivity, network use, output budget, approval, and `.cerebro` write risk while declaring `state_change=none`, `non-authoritative`, `advisory_allow_is_not_permission`, and `must_not_execute_automatically`. It is the prerequisite surface before MCP, Agents SDK tools, or external adapters; it is not a permission layer or runtime gate. Focused validation passed: capability policy `8/0`; lifecycle registration was added and rerun.
- Control-plane assessment slice closed on 2026-05-08: `experiments/control_plane_assessment/` is now the first concrete artifact of the `Cerebro Control Plane` front. It is a read-only report layer, not a scheduler or runtime gate. It composes existing `decision_runtime`, `epistemic_guard`, claim evaluation, and operational-signal outputs while declaring `state_change=none`, `non-authoritative`, `advisory_pass_is_not_permission`, and `must_not_execute_automatically`. Focused validation passed: assessment `6/0`, lifecycle `18/0`, runtime units `25/0`. The next control-plane growth decision should be based on this report layer, not on a storage migration by default.
- Growth front opened and closed on 2026-05-08: `FORMAL_RESUME_TRIGGER_CEREBRO_CONTROL_PLANE_GROWTH_001` recorded the front, rule-refactor protocol, anti-lixo rule, and deferred adapters. The docs-only opening validated with doc-governance `19/0` and architecture `51/0`.

- Docs-only reconciliation on 2026-05-08: `third-party-pilot-cycle-1` is resolved and rotated to `observation_center_archive.toml`. Human target selection confirmed `rpg_caminhada`; read-only evidence showed `rpg_caminhada/.cerebro/state.json` with ten curated sources and legacy state archived at `.cerebro_legacy_v1_2026-04-24`; same-source re-registration refreshed hashes with `added=0`, `removed=0`, `kept=10`; target `validate` and `analyze` then passed. Next target work must be target-local under `rpg_caminhada` rules, not Cerebro runtime growth.

- Third-party fiscal/accounting audit lesson recorded on 2026-04-28: `third-party-audit-reconciliation-lesson` is closed as a docs-only process correction. Cerebro's useful contribution was contradiction-seeking, not mere summation: it exposed that the final evidence could say `981` heads while the structured `qtd_cabecas` field summed `681`. The improvement is to require a hard reconciliation battery before future closeout language: totals by value, totals by structured quantities, narrative/history-vs-column consistency, expected documents vs found documents, excluded notes with reasons, PDFs without extracted keys, manual rules, and high-priority review rows. This does not open runtime growth or authorize target mutation; it only raises the bar for the next third-party audit closeout.

- Drift detection staleness integration passed on 2026-04-27: `experiments/drift_detection/staleness_scorer.py` is now wired into the detect pipeline. Baseline snapshot stores `captured_at` ISO timestamp (v2 format, backward-compat with v1 array). `detector.detect()` accepts optional `baseline_captured_at`; when present, computes deterministic staleness score (time 60% + structural changes 40%, ceiling 90 days / 5 changes) and classification (fresh/aging/stale/critical). Report markdown and JSON include staleness section. CLI `detect` and `status` commands display the score. 79 tests passing; architecture gate 51/51 green.

- Checkpoint semantic diff experiment created on 2026-04-27: `experiments/checkpoint_semantic_diff/` is a new non-authoritative derived track. Reads `.cerebro/state.json` from a target project, extracts checkpoint text (goal+summary+next_step), reads registered source file content, and computes deterministic Jaccard token overlap between them. Classifies each source as high/medium/low/unavailable alignment. Emits non-authoritative markdown+JSON report. Never writes to `.cerebro/`. 41 tests passing (extractor, tokenizer, jaccard, classify_alignment, score_alignment, report format, end-to-end pipeline, non-mutation boundary).

## Historical Snapshot — 2026-04-25

- Verification artifact root shape hardening passed on 2026-04-25 under `FORMAL_RESUME_TRIGGER_VERIFICATION_ARTIFACT_ROOT_SHAPE_HARDENING`. The audit finding was concrete: an existing non-directory `artifacts/verification` root could be hidden as an empty healthy report. The dry-run now preserves `missing_artifact_root` for absence, but reports `invalid_artifact_root` with one `ambiguous_do_not_touch` entry and zero cleanup-eligible bytes for an invalid existing root. Full AGENTS-equivalent gate is now `941/0/0/6`. Next candidate: continue audit-driven runtime hardening only where the next finding is concrete; still do not open apply-retention without material pressure from a real initialized project.

- Verification artifact retention race hardening passed on 2026-04-25 under `FORMAL_RESUME_TRIGGER_VERIFICATION_ARTIFACT_RETENTION_RACE_HARDENING`. The follow-on audit finding was real: the dry-run report existed, but transient filesystem metadata failures during scan could still degrade observability. The report now classifies unreadable/disappearing entries as `ambiguous_do_not_touch` instead of cleanup eligible, and size traversal catches filesystem errors without mutating artifacts. Full AGENTS-equivalent gate is now `940/0/0/6`. Next candidate: continue audit-driven runtime hardening only where the next finding is concrete; do not open apply-retention unless a real initialized Cerebro project shows material cleanup pressure.

- Verification artifact retention report passed on 2026-04-25 under `FORMAL_RESUME_TRIGGER_VERIFICATION_ARTIFACT_RETENTION_REPORT`. The audit finding was real: verification artifacts were bounded per run, but the runtime had no deterministic dry-run view of accumulated retention pressure. `core/verification_runtime.py` now exposes `build_verification_artifact_retention_report(...)`, classifying verification artifact entries as `live_referenced`, `recent_unreferenced`, `cleanup_eligible`, or `ambiguous_do_not_touch` with count and byte totals. This is observability only: no artifact is deleted, moved, archived, truncated, or rewritten. Full AGENTS-equivalent gate is now `939/0/0/6`. Next candidate: a separate apply-retention slice only if the dry-run report shows meaningful pressure; otherwise continue the audit-driven runtime hardening queue.

- Verification process containment hardening passed on 2026-04-25 under `FORMAL_RESUME_TRIGGER_VERIFICATION_PROCESS_CONTAINMENT_HARDENING`. The audit finding was real: timeout handling bounded the direct subprocess call but did not explicitly contain the spawned process tree/group before recording evidence. `core/verification_runtime.py` now uses a bounded subprocess helper, creates process boundaries where supported, fails timeout checks closed with `command timed out`, keeps stdout/stderr artifacts under the existing `65536` byte budget, and records best-effort cleanup failures instead of hiding them. Windows uses `taskkill /T /F /PID ...` plus fallback kill; POSIX uses process-group kill plus fallback kill. This is reliability hardening, not a promise of perfect orphan cleanup. Full AGENTS-equivalent gate is now `936/0/0/6`. Next candidate: continue the audit-driven hardening queue, likely verification artifact retention pressure or another narrow runtime reliability slice under a separate trigger.

- Verification output budget hardening passed on 2026-04-25 under `FORMAL_RESUME_TRIGGER_VERIFICATION_OUTPUT_BUDGET_HARDENING`. The audit finding was real: verification commands could persist unbounded stdout/stderr artifacts, making evidence noisy, costly, and harder to audit. `core/verification_runtime.py` now bounds persisted stdout/stderr artifact text to `65536` UTF-8 bytes after redaction, appends a stable truncation marker, preserves small output byte-for-byte, keeps failed commands failed, and hashes the persisted stdout text. No schema or command policy semantics changed. Full AGENTS-equivalent gate is now `931/0/0/6`. Next candidate: continue the audit-driven hardening queue, likely command timeout/process-tree containment, artifact retention pressure, or another narrow verification/runtime reliability slice under a separate trigger.

- Command sandbox ignore hardening passed on 2026-04-25 under `FORMAL_RESUME_TRIGGER_COMMAND_SANDBOX_IGNORE_HARDENING`. The audit finding was real: verification sandbox clones and manifests could spend work and produce noisy evidence over local generated roots (`_local/`, `.git/`, `venv/`, `.tmp*/`, caches, build/dist). `core/command_sandbox.py` now shares one deterministic ignore policy between sandbox copy and manifest capture, while preserving ordinary workspace files and `.cerebro/`. Focused sandbox/verification tests passed `14/0`; architecture gate passed `51/0`; full AGENTS-equivalent gate is now `928/0/0/6`. Next candidate: continue the audit-driven hardening queue, likely output-size caps or another narrow verification/runtime reliability slice under a separate trigger.

- Runtime id path-segment hardening passed on 2026-04-25 under `FORMAL_RESUME_TRIGGER_RUNTIME_ID_PATH_SEGMENT_HARDENING`. The audit finding was real: `action.id` and `command_registry[].id` were path-composed in runtime artifact/trash/verification surfaces after only non-empty validation. `core/runtime_ids.py` now defines the shared path-segment-safe id policy; `action_runtime` blocks unsafe action ids before artifact creation; `validation` blocks unsafe command/action ids in persisted state. New regressions cover `../escape` and safe dotted/underscored ids. Full AGENTS-equivalent gate is now `926/0/0/6`. Next candidate: continue the audit-driven hardening queue, likely sandbox/manifest ignore for `_local/` and output-size caps, under separate narrow triggers.

- Third-party trigger conformance advanced on 2026-04-25 under `FORMAL_RESUME_TRIGGER_THIRD_PARTY_TRIGGER_TEMPLATE_CONFORMANCE_SLICE_4`. The strict trigger template is now executable as an advisory check: `check_third_party_trigger_template_conformance(...)` reports missing required fields, invalid enum values, missing narrative sections, and missing reviewer evidence before a future target trigger can be treated as review-ready. It does not read target files, mutate targets, rewrite history, or grant permission. Next candidate: use the template plus conformance check as the entry point for the next third-party trigger, or pause target work and continue Cerebro-side consolidation.
- Third-party trigger template advanced on 2026-04-25 under `FORMAL_RESUME_TRIGGER_THIRD_PARTY_TRIGGER_TEMPLATE_SLICE_3`. `THIRD_PARTY_TRIGGER_TEMPLATE.md` is now the standard drafting surface for future third-party project triggers. It requires structured `third_party`, `source_roles`, `boundaries`, and `risk_budget` blocks; separates target product value from Cerebro dogfood value; requires reviewer evidence before execution; and preserves the three-target-mutating-slices consolidation stop. Next candidate: either add a tiny template-conformance check around this document, or use the template as the entry point for the next third-party project trigger.
- Third-party trigger retrospective advanced on 2026-04-25 under `FORMAL_RESUME_TRIGGER_THIRD_PARTY_TRIGGER_RETROSPECTIVE_SLICE_2`. The reviewer was calibrated against the real `rpg_caminhada` trigger history: legacy wording like `Allowed target files under ...` and `Explicit Prohibitions` is now recognized, and a multi-trigger retrospective helper summarizes multiple advisory reviews without gaining authority. The retrospective reviewed `25` historical triggers and found the real management gap: the historical series was bounded and validated, but it predates mandatory structured fields for `slice_kind`, `dogfood_value`, `proof_cost`, and source roles; `14/25` would now force consolidation under the three-target-slices stop line. Next candidate: create a strict third-party trigger template/checklist from the runbook plus this retrospective.
- Third-party trigger review advanced on 2026-04-25 under `FORMAL_RESUME_TRIGGER_THIRD_PARTY_TRIGGER_REVIEW_SLICE_1`. New `experiments/third_party_trigger_review/` reviews caller-supplied third-party trigger text before execution and reports missing/risky fields without granting permission. It checks `target_path`, `slice_kind`, `dogfood_value`, `proof_cost`, source roles, target `.cerebro/` handling, rollback, cleanup, stop conditions, forbidden paths, consecutive target-slice consolidation risk, and runtime boundary drift. Focused tests passed `6/0`; architecture/doc-governance passed `64/0`. No runtime or target mutation occurred. Next candidate: apply this reviewer to the existing `rpg_caminhada` trigger history to produce a retrospective quality report, or convert the runbook/reviewer into a stricter third-party trigger template.
- Third-party dogfood lessons were consolidated on 2026-04-25 under `FORMAL_RESUME_TRIGGER_THIRD_PARTY_DOGFOOD_LESSONS_CONSOLIDATION`. `THIRD_PARTY_DOGFOOD_LESSONS.md` records that `rpg_caminhada` proved Cerebro can manage real external work, but also proved the main risk is role drift: without a stop line, the agent becomes product developer for the target. `THIRD_PARTY_PROJECT_MANAGEMENT_RUNBOOK.md` now defines recon, intake gate, source-set classification, target runtime handling, applied target slice, consolidation stop, and Cerebro hardening decision. It also adds concrete trigger/report fields: `slice_kind`, `dogfood_value`, `proof_cost`, `cleanup_required`, source roles, target `.cerebro/` classification, and mandatory consolidation after three consecutive target-mutating slices. No runtime or third-party target mutation occurred. Next candidate: a Cerebro-side hardening slice that turns this runbook into a stricter third-party trigger template/checklist or advisory checker.
- `rpg_caminhada` enemy decision readability passed on 2026-04-25 under `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_ENEMY_DECISION_READABILITY_SLICE_1`. The target combat screen now explains why the selected enemy is worth choosing before execution through `createEnemyDecisionSummary(...)`: cost, risk, return, and recommended plan. In-app browser proof at `http://localhost:19006/` opened `Combate`, found `Por que este alvo?`, `Custo por rodada`, `Risco`, `Retorno`, and `Plano recomendado`, then selected `Lobo Selvagem` and observed `5 Vigor por luta · 15 no lote atual` plus `Poder 114 vs 48`. Target typecheck is green. No backend function, schema migration, generated types, dependency/env/cloud, target `.cerebro/`, enemy balance, reward math, progression system, or Cerebro runtime mutation occurred. Next candidate: stop extending the third-party product for now and open a Cerebro-side dogfood lessons consolidation trigger.
- `rpg_caminhada` inventory reward follow-through passed on 2026-04-25 under `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_INVENTORY_REWARD_FOLLOWTHROUGH_SLICE_1`. The target `Bolsa` now distinguishes combat reward receipt from inventory confirmation through `createRewardFollowthrough(...)`. After `Combate -> Executar combate -> Ver bolsa`, the top `Recompensa recente` block shows `Recebido na ultima rodada`, origin, combat/vigor receipt facts, XP/ouro/item counts, and per-drop status. Browser proof at `http://localhost:19006` with `EXPO_NO_DOTENV=1` found `Origem: Slime de Musgo`, `3 combates`, `9 Vigor gasto`, `Recibo de combate visivel`, `XP 20`, `OURO 24`, `ITENS 2`, `slime_musgo_loot x2`, `PREVIEW LOCAL`, and `Recebido no combate desta sessao; preview nao prova persistencia`. Target typecheck is green. No backend function, schema migration, generated types, dependency/env/cloud, target `.cerebro/`, or Cerebro runtime mutation occurred. Next candidate: choose between enemy/progression readability or a backend-backed inventory confirmation proof under a separate trigger.
- `rpg_caminhada` combat reward readability passed on 2026-04-25 under `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_COMBAT_REWARD_READABILITY_SLICE_1`. The target combat screen now turns the already-returned reward payload into a readable gameplay result through `createCombatResultSummary(...)`: outcome, enemy, combat record, vigor delta, XP, gold, item drops, and next-action copy. Browser proof at `http://localhost:19006` with `EXPO_NO_DOTENV=1` executed `Combate -> Executar combate` and found `Resultado da rodada`, `Rodada vencida`, `Contra Slime de Musgo`, `3 combates resolvidos`, `2 vitorias`, `1 derrotas`, `Vigor -9`, `restante 3`, `+20 XP`, `+24 ouro`, `slime_musgo_loot x2`, `Ver campanha`, `Ver bolsa`, and `Sincronizar atividade`. Target typecheck is green. No backend function, schema migration, generated types, dependency/env/cloud, target `.cerebro/`, or Cerebro runtime mutation occurred. Next candidate: choose one gameplay loop improvement that builds on combat clarity, likely inventory reward follow-through or enemy progression readability under a separate trigger.
- `rpg_caminhada` combat result evidence UI passed on 2026-04-25 under `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_COMBAT_RESULT_EVIDENCE_UI_SLICE_1`. The target combat screen now shows the already-returned `combat_stat_snapshot` after resolution: stat equations, active equipment source rows, and player/enemy power values. Preview browser proof at `http://localhost:19006` with `EXPO_NO_DOTENV=1` executed `Combate -> Executar combate` and found `Evidencia do combate`, `FORCA 3 +0 = 3`, `RESISTENCIA 2 +1 = 3`, `Botas de trilha: Resistencia +1`, `Voce 114.4`, and `Inimigo 46.8`. Target typecheck is green. No backend function, schema migration, generated types, dependency/env/cloud, target `.cerebro/`, or Cerebro runtime mutation occurred. Next candidate: pick a gameplay loop improvement that compounds this visible equipment-to-combat chain, preferably enemy reward readability or small combat result polish under a separate trigger.
- `rpg_caminhada` combat effective stats integration passed on 2026-04-24 local / 2026-04-25 UTC under `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_COMBAT_EFFECTIVE_STATS_SLICE_1`. The target now shares one `stat_projection` helper between inventory and manual combat, so combat uses `effective_stats` derived from base profile attributes plus equipped item bonuses. `resolve-manual-combat` returns `combat_stat_snapshot` with `base_stats`, `equipment_modifiers`, `effective_stats`, and contributing `sources`. Local regression proved baseline combat strength modifier `0`, equipped combat strength modifier `2`, equipped effective strength `4`, baseline `playerPower=76.72`, equipped `playerPower=98.72`, source `Cajado de Caminhada: strength +2`, loot quantity `1 -> 2`, same-slot replacement, and unequip restoring modifier `0`. No schema migration, generated types, dependency/env/cloud, target `.cerebro/`, or Cerebro runtime mutation occurred. Next candidate: expose combat stat evidence in the target UI or make the combat result screen more readable, under a separate trigger.
- `rpg_caminhada` equipment stats projection passed on 2026-04-24 local / 2026-04-25 UTC under `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_EQUIPMENT_STATS_PROJECTION_SLICE_1`. `get-player-inventory` now returns `stat_projection` with `base_stats`, `equipment_modifiers`, `effective_stats`, and contributing `sources`, derived from base profile stats plus equipped item bonuses. `Bolsa` renders `Atributos efetivos` as base + modifier = effective. Local regression proved strength modifier `0 -> 1 -> 2 -> 0` and effective strength `4` after same-slot replacement. Chrome headless DOM proof observed `FORCA 2 +0 = 2`, clicked `Equipar`, then observed `FORCA 2 +2 = 4` and `Cajado de Caminhada: strength +2`. No combat stat integration, schema migration, generated types, dependency/env/cloud, target `.cerebro/`, or Cerebro runtime mutation occurred. Next candidate: combat effective stats integration.
- `rpg_caminhada` equipment runtime passed on 2026-04-24 local / 2026-04-25 UTC under `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_EQUIPMENT_RUNTIME_SLICE_1`. The target now has persisted equip/unequip over existing `player_inventory.is_equipped`: non-owned rows fail closed, stackable material loot is not equippable, owned non-stackable weapons/armor equip, same-slot replacement unequips the prior item, and unequip clears the selected row. `get-player-inventory` now projects `items`, `equipped`, and `equipment_summary`; `Bolsa` renders equipment slots and backend controls. Local regression proved non-owned `404`, first weapon equip, second weapon replacement, one active weapon after replacement, zero after unequip, and the prior persisted loot quantity checks still pass. Chrome headless DOM proof rendered `Bolsa`, clicked `Equipar`, and observed `ARMA Cajado de Caminhada` plus `Desequipar`. No schema migration, generated types, combat stat integration, dependency/env/cloud, target `.cerebro/`, or Cerebro runtime mutation occurred. Next candidate: derived equipment stats projection before combat reads equipment.
- `rpg_caminhada` persisted inventory passed on 2026-04-24 local / 2026-04-25 UTC under `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_PERSISTED_INVENTORY_SLICE_1`. Manual combat now persists item rewards into existing inventory tables: missing item codes become deterministic `inventory_items`, first acquisition inserts `player_inventory`, repeated acquisition increments quantity, and `get-player-inventory` exposes authenticated inventory metadata. Local regression proved `goblin_fraco_z1_loot` quantity `1` after first combat and `2` after second. Expo web proof with local Supabase auth rendered `Bolsa` as `Inventario persistido` and showed `Goblin Fraco x2`. Target `npm run -s typecheck` is green. No target `.cerebro/`, schema migration, generated type edit, dependency/env edit, cloud deploy, or Cerebro runtime mutation occurred. Next candidate: persisted-inventory UX polish or a separate equipment mechanics trigger.
- `rpg_caminhada` inventory/loot feedback passed on 2026-04-24 local / 2026-04-25 UTC under `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_INVENTORY_LOOT_FEEDBACK_SLICE_1`. The target now has a `Bolsa` tab that is explicitly client-only/advisory, shows dashboard-derived progression signals, projected equipment/items, and latest-session combat loot. Browser proof at `http://localhost:19006` with `EXPO_NO_DOTENV=1` rendered the tab, executed `Combate -> Executar combate -> Ver bolsa`, and confirmed `Slime de Musgo`, XP, gold, and item feedback in the inventory surface. Target `npm run -s typecheck` is green. No target `.cerebro/`, source import, schema/migration edit, generated DB type edit, Edge Function behavior edit, env edit, dependency addition, cloud link/deploy, or Cerebro runtime change occurred. Next candidate: choose between more client-only inventory feel or a separate backend/schema trigger for persisted inventory.
- `rpg_caminhada` campaign map visual proof/polish passed on 2026-04-24 local / 2026-04-25 UTC under `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_CAMPAIGN_MAP_VISUAL_PROOF_SLICE_1`. Expo web rendered the `Mapa` tab at `http://localhost:19006` with `EXPO_NO_DOTENV=1` and preview dashboard data. The proof confirmed zone, route, vigor, unlock, roadmap, objectives, and advisory local-roadmap copy; it also found and fixed real UI issues: the five-tab header became a horizontal rail, objective labels now fit, and a weak synthetic evolution meter was replaced with factual next-action guidance. Target `npm run -s typecheck` is green after polish; final full AGENTS-equivalent gate is green at `923/0/0/6`. No target `.cerebro/`, source import, schema/migration edit, generated DB type edit, Edge Function behavior edit, env edit, dependency addition, cloud link/deploy, or Cerebro runtime change occurred. Next candidate: small inventory/loot feedback surface.
- `rpg_caminhada` campaign map slice passed on 2026-04-24 local / 2026-04-25 UTC under `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_CAMPAIGN_MAP_SLICE_1`. The target now has a `Mapa` gameplay tab that uses only existing dashboard data to show current zone, kilometers, level, vigor readiness, daily mission posture, next local roadmap unlock, and bounded MVP zone roadmap. Target `npm run -s typecheck` is green; final full AGENTS-equivalent gate is green at `923/0/0/6`. No target `.cerebro/`, source import, schema/migration edit, generated DB type edit, Edge Function behavior edit, env edit, dependency addition, cloud link/deploy, or Cerebro runtime change occurred. Next candidate: visual proof/polish of the map tab in the local app UI before adding another gameplay system.
- `rpg_caminhada` sync idempotency harness passed on 2026-04-24 local / 2026-04-25 UTC under `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_SYNC_IDEMPOTENCY_HARNESS_SLICE_1`. The target now exposes `npm run -s test:sync-idempotency:local`, a no-new-dependency local harness that creates a disposable local user and proves sync replay/no-pending reward behavior: first sync `inserted=2/duplicates=0/rejected=1`, replay sync `inserted=0/duplicates=2/rejected=1`, persisted session count remains `2`, first rewards `sessions_processed=2`, second rewards `status=no_pending_sessions` with `sessions_processed=0`, `pa_total_earned=0`, and `vigor_earned=0`, and persisted rewards remain `2`. Target typecheck/status are green, db reset cleaned proof data, and final full AGENTS-equivalent gate is green at `923/0/0/6`. No target `.cerebro/`, source import, schema/migration edit, generated DB type edit, Edge Function behavior edit, app UI edit, cloud link/deploy, dependency addition, or Cerebro runtime change occurred. Next candidate: broader gameplay surface.
- `rpg_caminhada` refresh-after-combat polish passed on 2026-04-24 local / 2026-04-25 UTC under `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_REFRESH_AFTER_COMBAT_SLICE_1`. The app already had `dashboardRefreshToken` and `useDashboard(refreshToken)` reload support; the missing link was combat. The target now exposes `registerCombatResolved()` in `GameFlowContext`, `useCombatPreview().runCombat()` returns success/failure and tracks `hasResolvedCombat`, and `CombatScreen` increments the dashboard refresh token after a successful combat plus shows `Ver campanha atualizada` to route back to the refreshed campaign dashboard. Target `npm run -s typecheck` is green; final full AGENTS-equivalent gate is green at `923/0/0/6`. No target `.cerebro/`, source import, schema/migration edit, generated DB type edit, Edge Function behavior edit, cloud link/deploy, dependency addition, or Cerebro runtime change occurred. Next candidate: add one target-local duplicate/no-pending sync harness before broader gameplay surface.
- `rpg_caminhada` backend regression harness passed on 2026-04-24 local / 2026-04-25 UTC: `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_BACKEND_REGRESSION_HARNESS_SLICE_1` is consumed. The target now exposes `npm run -s test:function-chain:local`, a no-new-dependency local harness that creates a disposable local user and asserts the function chain: sign-up, onboarding, sync, rewards, dashboard, enemies, combat, dashboard, and table consistency. Proof run passed with `sync.inserted=2`, `sync.rejected=1`, `rewards.sessions_processed=2`, `combat.combats_resolved=3`, `combat.vigor_remaining=25`, `after_combat.profile.current_vigor=25`, `after_combat.daily.current_vigor=25`, `after_combat.daily.combats_manual_count=3`, and dashboard vigor `25`. Cleanup DB reset passed, temporary function serve was stopped, target typecheck/status stayed green, and no target `.cerebro/`, source import, schema/migration edit, generated DB type edit, Edge Function behavior edit, app UI edit, cloud link/deploy, or Cerebro runtime change occurred. Next item: choose between in-app refresh-after-combat polish or one more duplicate/no-pending sync harness before adding gameplay surface.
- `rpg_caminhada` state-consistency review passed on 2026-04-24 local / 2026-04-25 UTC: `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_STATE_CONSISTENCY_REVIEW_SLICE_1` is consumed. The observed post-combat divergence was reproduced as a real bug: after rewards, profile and daily vigor were both `34`; after a three-combat batch, profile/dashboard vigor became `25` while `daily_player_state.current_vigor` stayed `34` and `combats_manual_count` stayed `0`. The target `resolve-manual-combat` endpoint now updates or creates the same-day daily snapshot after successful profile vigor update. Post-patch reproduction showed profile vigor `25`, daily vigor `25`, `daily_player_state.combats_manual_count=3`, dashboard current vigor `25`, and `fixed=true`. Cleanup `npx supabase db reset` passed, temporary function serve was stopped, target typecheck/status stayed green, and no target `.cerebro/`, source import, schema/migration edit, generated DB type edit, cloud link/deploy, or Cerebro runtime change occurred. Next item: target-local backend regression harness for this function chain and consistency class.
- `rpg_caminhada` manual UI proof passed on 2026-04-24 local / 2026-04-25 UTC: `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_MANUAL_UI_PROOF_SLICE_1` is consumed. The slice installed missing Expo web runtime dependencies (`react-dom`, `react-native-web`), added `babel-preset-expo`, fixed the root `SafeAreaProvider`, then proved the local app path in browser: sign-up, onboarding, sync without health permissions, rewards, and combat. DB evidence before cleanup: `player_profiles=1`, `activity_sources=1`, `activity_sessions=2`, `activity_rewards=2`, `combat_encounters=1`, `daily_player_state=1`. Temporary servers were stopped, `npx supabase db reset` passed, and target typecheck/status stayed green. No target `.cerebro/`, source import, schema/migration edit, generated DB type edit, Edge Function behavior edit, cloud link/deploy, or Cerebro runtime change occurred. Next item: state-consistency review around post-combat vigor/dashboard/daily snapshot semantics.
- `rpg_caminhada` app/runtime integration slice passed on 2026-04-24 local / 2026-04-25 UTC: `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_APP_RUNTIME_INTEGRATION_SLICE_1` is consumed. The app's Supabase client now accepts the local Expo public key names produced by Supabase CLI, `.env.local` points to the local Supabase runtime, and the no-health-permission demo sync path now generates per-run external session ids for a real `source_id`. Target `npm run -s typecheck`, `npx supabase status`, architecture/doc governance (`51/0`, `13/0`), and full AGENTS-equivalent gate (`923/0/0/6`) are green. No target `.cerebro/`, source import, schema/migration edit, generated DB type edit, Edge Function behavior edit, cloud link/deploy, or Cerebro runtime change occurred. Next item is a manual UI proof trigger: run local functions serve + Expo and prove auth/onboarding, sync, rewards, and combat through the app screens.
- `rpg_caminhada` mutational Edge Function smoke passed on 2026-04-24 local / 2026-04-25 UTC: `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_EDGE_FUNCTION_MUTATIONAL_SMOKE_SLICE_2` is consumed. The slice reused the local Edge Function environment, created and signed in a local auth user, onboarded the user, then proved the backend mutation path: `sync-activities` returned HTTP `200` with `inserted=1`, `duplicates=1`, `rejected=1`; `process-activity-rewards` returned HTTP `200` with `sessions_processed=1`, `pa_total_earned=28.8`, `vigor_earned=4.8`; `list-enemies` returned HTTP `200` with one available zone-1 tier-0 template; `resolve-manual-combat` returned HTTP `200` with `combats_resolved=1`, `vigor_spent=3`, `vigor_remaining=11.8`. Local table evidence was captured before cleanup (`activity_sessions=1`, `activity_rewards=1`, `combat_encounters=1`, `daily_player_state=1`), the temporary serve process was stopped, `npx supabase db reset` passed, and target typecheck/status stayed green. No target `.cerebro/`, source import, schema/migration edit, generated DB type edit, Edge Function behavior edit, cloud link, deployment, app UI work, or Cerebro runtime change occurred. Backend smoke unblocking is done; next item is a separate app/runtime integration trigger with explicit UI/device boundary.
- `rpg_caminhada` first Edge Function smoke passed on 2026-04-24 local / 2026-04-25 UTC: `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_EDGE_FUNCTION_SMOKE_SLICE_1` is consumed. The slice recovered a local Docker Desktop API 500 by restarting Docker/WSL, generated `.env.functions`, ignored it in target `.gitignore`, served local functions, created and signed in a local auth user, and proved `onboard-player`, `get-player-dashboard`, and `list-enemies` all return HTTP `200`. It then stopped the function server and ran `npx supabase db reset` to clean smoke data. No target `.cerebro/`, source import, schema/migration edit, generated DB type edit, Edge Function behavior edit, cloud link, deployment, app UI work, or Cerebro runtime change occurred. Next item is a second local-only smoke for heavier mutational functions with explicit fixtures.
- `rpg_caminhada` Docker environment unblocked on 2026-04-24 local / 2026-04-25 UTC: `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_DOCKER_ENVIRONMENT_UNBLOCK` is consumed. Docker Desktop 4.70.0 was installed via `winget`, `com.docker.service` was started, Docker CLI/server `29.4.0` became available, target `npm run -s typecheck` stayed green, `npx supabase status` passed, `npx supabase start` passed, and `npx supabase db reset` passed. The local reset applied the four migrations from `20260405000000` through `20260405000003`. No target `.cerebro/`, source import, schema/migration edit, generated DB type edit, Edge Function behavior edit, cloud link, or deployment occurred. Next item is a separate Edge Function environment/serve/smoke trigger.
- `rpg_caminhada` Supabase smoke-validation attempted on 2026-04-24 local / 2026-04-25 UTC: `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_SUPABASE_SMOKE_VALIDATION` is consumed with a concrete blocker. The target `npm run -s typecheck` remains green and `npx supabase --version` works (`2.84.10`). The first CLI blocker was `supabase/config.toml` parse failure because `[db].enabled` is invalid; the slice removed only that local-tooling compatibility key. Afterward, `npx supabase status`, `npx supabase start`, and `npx supabase db reset` all reached the same infrastructure blocker: Docker daemon unavailable (`docker` not on PATH; `//./pipe/docker_engine` missing). No target `.cerebro/`, schema, migration, Edge Function behavior, source import, cloud link, or deployment was touched.
- `rpg_caminhada` validation-unblock patch executed on 2026-04-24 local / 2026-04-25 UTC: `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_VALIDATION_UNBLOCK_SLICE_1` is consumed. The target `npm run -s typecheck` is now green. Patch stayed within validation unblock: add `react-native-health` / `react-native-health-connect`, fix `AuthContext.tsx` source-id narrowing, type the iOS HealthKit bridge, remove invalid iOS `Walking` permission, and document `SUPABASE_ANON_KEY` for local Edge Functions. Target `.cerebro/` was not touched and no Supabase smoke validation was run.
- `rpg_caminhada` next-work plan produced on 2026-04-24 local / 2026-04-25 UTC: `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_NEXT_WORK_PLAN` is consumed. `RPG_CAMINHADA_NEXT_WORK_PLAN.md` concluded that the target did not need schema/formula recreation or blank Edge Function implementation. It identified the now-consumed validation-unblock patch as the first real target edit before smoke validation.
- `DECISIONS.md` source-set recovery executed on 2026-04-24 local / 2026-04-25 UTC: `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_DECISIONS_IMPORT_RECOVERY` is consumed. The first import attempt for `cerebro_base/DECISIONS.md` produced the expected one-file diff but exposed a real pilot hygiene issue: the prior target session was active without a preserved token, so mutation was correctly blocked. Recovery preserved the blocked v2 `.cerebro/`, recreated a fresh canonical v2 runtime, imported exactly the 10 approved sources, checkpointed, validated, ran `analyze`, discarded the emitted session token, and left no tokenless active session behind. Result: `sources=10`, `DECISIONS.md` registered, `validation=ok`, target source files untouched.
- Third-party pilot executed on 2026-04-24: `FORMAL_RESUME_TRIGGER_THIRD_PARTY_PILOT` is consumed. `D:\projetos_cli\pessoais\rpg_caminhada` now has a fresh canonical v2 `.cerebro/`; the legacy schema-v1 `.cerebro/` was preserved as `.cerebro_legacy_v1_2026-04-24`; `9` approved primary sources were imported; `checkpoint`, `validate`, and `analyze` succeeded; `context_discovery` produced `20` advisory candidates with `0` drift and `0` missing registered sources. The pilot proves the existing runtime can manage a real external project; the next decision is source-candidate triage, not automatic import.
- Source triage completed on 2026-04-24: `FORMAL_RESUME_TRIGGER_RPG_CAMINHADA_SOURCE_TRIAGE` is consumed. `THIRD_PARTY_SOURCE_TRIAGE_RPG_CAMINHADA.md` recommends importing exactly one additional source, `cerebro_base/DECISIONS.md`, then validating/analyzing again. Everything else from the `20` discovery candidates stays deferred or excluded for now: templates, empty scaffolds, derived panels, Obsidian tooling, generic modes, and legacy local `VERSAO_DO_CEREBRO.md`.
- Epistemic-runtime lane applied to a real decision on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_GUARD_APPLIED_THIRD_PARTY_DECISION_SLICE_8` is consumed. The checked-in manifest/report/packet/stress-repro/closeout artifacts evaluate whether to resume third-party pilot planning now. Real output: report `advisory_report_allowed/none/0/0`, packet `go_for_advisory_review/advisory_report_allowed/none/0`, stress/repro `10/10/0`, closeout `closed_until_new_evidence/no_action/0`, `state_change=none`. Decision: planning can resume at the advisory layer, but no target `.cerebro/` mutation is authorized. Next concrete step is human approval or amendment of the `rpg_caminhada` intake gate: target, legacy state handling, and exact source set.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_GUARD_PRE_ACTION_PACKET_REVIEW_CLOSEOUT_SLICE_7` is consumed. `experiments/epistemic_guard/` now has an advisory closeout over the pre-action decision packet and packet stress/repro report. Real output: `closeout_status=closed_until_new_evidence`, `action_readiness=no_action`, `recommended_human_decision=none`, `recursive_hardening_stopped=true`, `input_count=2`, `blocker_count=0`, `missing_review_evidence_count=0`, `stress_repro_case_count=10`, `stress_repro_fail_count=0`, `reopen_trigger_count=5`, and `state_change=none`. This stops the current pre-action packet recursion until new evidence appears; closeout/no-action is not permission.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_GUARD_PRE_ACTION_PACKET_STRESS_REPRO_SLICE_6` is consumed. `experiments/epistemic_guard/` now has advisory packet-level stress/repro evidence for the pre-action decision packet. Real output: `case_count=10`, `pass_count=10`, `fail_count=0`, `blocked_case_count=7`, `human_review_case_count=1`, `reproducible_case_count=1`, `mismatch_case_count=3`, `boundary_error_count=2`, and `state_change=none`. It proves clean, blocked, human-review, failed-stress, reproducible, stale, malformed, missing, root-escaping, and `.cerebro`-target packet artifacts remain visible and blocked where appropriate. Packet stress/repro is review evidence only; it is not permission.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_GUARD_PRE_ACTION_DECISION_PACKET_SLICE_5` is consumed. `experiments/epistemic_guard/` now has an advisory pre-action decision packet combining the pre-action report and stress matrix into one operator-facing posture. Real output: `operator_posture=go_for_advisory_review`, `action_readiness=derived_experiment_allowed`, `recommended_human_decision=none`, `packet_blocker_count=0`, `stress_case_count=6`, `stress_fail_count=0`, `stress_blocked_or_human_count=5`, and `state_change=none`. The packet is review compression only; it is not permission.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_GUARD_PRE_ACTION_STRESS_SLICE_4` is consumed. `experiments/epistemic_guard/` now has an advisory pre-action stress matrix. Real output: `case_count=6`, `pass_count=6`, `fail_count=0`, `blocked_or_human_count=5`, `boundary_error_count=2`, and `state_change=none`. It proves the clean pre-action path stays advisory-only, while missing `[proposed_action]`, mutating expected state, runtime promotion without trigger, stale approval, and read/write drift stay blocked or require trigger/human review. Stress pass is not permission.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_GUARD_PRE_ACTION_REPORT_SLICE_3` is consumed. `experiments/epistemic_guard/` now turns a required `[proposed_action]` TOML table plus declared decision-envelope evidence into one concrete advisory `PreActionGuardReport`. Real self-report output: `envelope_count=1`, `blocked_or_human_count=0`, `action_readiness=derived_experiment_allowed`, `recommended_human_decision=none`, and `state_change=none`. This is the first operator-style pre-action surface; it is still not execution, approval, permission, source registration, memory, runtime gate, canonical claim graph, or authority.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_GUARD_DECISION_MANIFEST_SLICE_2` is consumed. `experiments/epistemic_guard/` now loads checked-in TOML action manifests and regenerates advisory decision-envelope reports. Real self-manifest output: `3` scenarios, `2` blocked-or-human-required cases, `0` advisory-report-allowed cases, `1` derived-experiment-allowed case, and `state_change: none`. The manifest proves the next layer can carry explicit reads/writes/claims/requirements/approval/prewrite evidence without turning manifest presence into permission.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_GUARD_DECISION_ENVELOPE_ORACLE_SLICE_1` is consumed. `experiments/epistemic_guard/` now evaluates concrete action questions through deterministic advisory `DecisionEnvelope` output. Real self-oracle output: `8` scenarios, `7` blocked-or-human-required cases, `1` advisory-allowed case, and `state_change: none`. Covered blockers include stale next action, silence-as-missing-evidence, existing state ambiguity, missing runtime trigger, approval expiry by source-set change, read/write drift, and protocol-induced stale source routing. This is the first applied guard after the closeout lane; it is not permission, truth, memory, source registration, runtime gate, canonical claim graph, or authority.
- Epistemic-runtime lane consolidated on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_RUNTIME_POSITION_CONSOLIDATION_SLICE_34` is consumed. `docs/handoffs/HANDOFF_EPISTEMIC_RUNTIME_POSITION_AFTER_CLOSEOUT.md` now records the restart point: the current operator-evidence recursion lane is closed until new evidence, and the next useful frontier is applied advisory decision-envelope evidence over a concrete action question. Do not keep indexing the index; reopen only for real blocker, mismatch, new operator decision surface, human-approved promotion question, or applied oracle failure.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_OPERATOR_EVIDENCE_CHAIN_CLOSEOUT_SLICE_33` is consumed. `experiments/epistemic_readiness/` now has a deterministic advisory closeout over the operator-facing evidence chain. Real output: `closeout_status=closed_until_new_evidence`, `recommended_human_decision=none`, `action_readiness=no_action`, `recursive_hardening_stopped=true`, `input_count=3`, `blocker_count=0`, and `missing_evidence_count=0`. This explicitly stops recursive hardening of the current derived evidence lane until a real blocker, mismatch, new operator decision surface, or human-approved promotion question appears; closeout/no-action remains separate from permission, truth, memory, authority, runtime gate, source registration, canonical graph, claim graph, automatic refresh, and permanent freeze.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_OPERATOR_EVIDENCE_FINAL_REVIEW_INDEX_STRESS_REPRODUCIBILITY_SLICE_32` is consumed. `experiments/epistemic_readiness/` now has a deterministic advisory reproducibility checker for the checked-in operator evidence final review index stress matrix artifacts. Real output: `reproducibility_status=reproducible`, `recommended_human_decision=none`, `action_readiness=advisory_report_allowed`, `artifact_count=2`, `blocker_count=0`, `mismatch_count=0`, `missing_artifact_count=0`, `json_digest_match=true`, and `markdown_digest_match=true`. This proves the final review index stress matrix JSON/Markdown are current against regenerated output while keeping reproducibility, digest equality, stress pass, and review-clear separate from permission, truth, memory, authority, source registration, runtime gate, canonical graph, claim graph, and automatic refresh.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_OPERATOR_EVIDENCE_FINAL_REVIEW_INDEX_STRESS_SLICE_31` is consumed. `experiments/epistemic_readiness/` now has a deterministic advisory stress matrix over the operator evidence final review index. Real output: `scenario_count=10`, `pass_count=10`, `fail_count=0`, `all_scenarios_passed=true`, `blocker_count=19`, `degraded_blocker_count=19`, `input_blocker_count=19`, `missing_review_evidence_count=3`, and `boundary_error_count=2`. Clean final review evidence remains `none/advisory_report_allowed`; every degraded scenario becomes `review_blockers/blocked`. This hardens the final review surface without making stress pass, digest equality, review-clear, or reproducibility into permission.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_OPERATOR_EVIDENCE_FINAL_REVIEW_INDEX_SLICE_30` is consumed. `experiments/epistemic_readiness/` now has a deterministic advisory final review index over the operator evidence review capsule, review capsule stress matrix, and review capsule reproducibility check. Real output: `review_status=final_review_clear`, `recommended_human_decision=none`, `action_readiness=advisory_report_allowed`, `input_count=3`, `input_blocker_count=0`, `blocker_count=0`, `missing_review_evidence_count=0`, `capsule_review_status=review_clear`, stress `9/9/0`, `stress_all_scenarios_passed=true`, `reproducibility_status=reproducible`, `json_digest_match=true`, and `markdown_digest_match=true`. This gives one final inspectable operator evidence surface while keeping the index separate from permission, truth, memory, authority, source registration, runtime gate, canonical graph, claim graph, and automatic refresh.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_OPERATOR_EVIDENCE_REVIEW_CAPSULE_REPRODUCIBILITY_SLICE_29` is consumed. `experiments/epistemic_readiness/` now has a deterministic advisory reproducibility checker for checked-in operator evidence review capsule artifacts. Real output: `reproducibility_status=reproducible`, `recommended_human_decision=none`, `action_readiness=advisory_report_allowed`, `artifact_count=2`, `blocker_count=0`, `mismatch_count=0`, `missing_artifact_count=0`, `json_digest_match=true`, and `markdown_digest_match=true`. This proves checked-in review capsule JSON/Markdown are current against regenerated output while keeping reproducibility separate from permission, truth, memory, authority, source registration, runtime gate, canonical graph, claim graph, and automatic refresh.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_OPERATOR_EVIDENCE_REVIEW_CAPSULE_STRESS_SLICE_28` is consumed. `experiments/epistemic_readiness/` now has a deterministic advisory stress matrix over the operator evidence review capsule. Real output: `scenario_count=9`, `pass_count=9`, `fail_count=0`, `all_scenarios_passed=true`, `blocker_count=15`, `degraded_blocker_count=15`, `input_blocker_count=5`, `missing_review_evidence_count=4`, and `boundary_error_count=2`. Decisions are: `clean_review_capsule=none/advisory_report_allowed`; missing packet, malformed reproducibility, mutating provenance, root escape, `.cerebro` target, stale reproducibility, failed upstream stress, and provenance blocker inputs all become `review_blockers/blocked` with visible review blockers. This hardens the final operator review surface without making it permission.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_OPERATOR_EVIDENCE_REVIEW_CAPSULE_SLICE_27` is consumed. `experiments/epistemic_readiness/` now has a deterministic advisory review capsule over decision packet, intake reproducibility, provenance index, and provenance stress evidence. Real output: `review_status=review_clear`, `recommended_human_decision=none`, `action_readiness=advisory_report_allowed`, `input_count=4`, `input_blocker_count=0`, `blocker_count=0`, `missing_review_evidence_count=0`, `decision_posture=no_action`, `reproducibility_status=reproducible`, `digest_match=true`, provenance `20/20` present with `39` dependency edges, and stress coverage `9/9/0` with `7` degraded blockers, `4` boundary errors, and `1` text digest-only case. This gives the operator one compact review surface without making it permission.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_OPERATOR_EVIDENCE_PROVENANCE_STRESS_SLICE_26` is consumed. `experiments/epistemic_readiness/` now has a deterministic advisory stress matrix over the provenance index. Real output: `scenario_count=9`, `pass_count=9`, `fail_count=0`, `all_scenarios_passed=true`, `blocker_count=7`, `boundary_error_count=4`, `text_digest_only_count=1`. Clean and markdown/text-only digest scenarios remain `none/advisory_report_allowed`; missing artifact, malformed JSON, mutating artifact, root escape, `.cerebro` target, duplicate id, and missing dependency all become `review_blockers/blocked`. This proves degraded provenance evidence stays visible instead of becoming permission.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_OPERATOR_EVIDENCE_PROVENANCE_INDEX_SLICE_25` is consumed. `experiments/epistemic_readiness/` now has a deterministic advisory provenance index over the checked-in operator-facing evidence chain. Real output: `artifact_count=20`, `present_count=20`, `dependency_edge_count=39`, `blocker_count=0`, `recommended_human_decision=none`, `action_readiness=advisory_report_allowed`, and digest manifest `d254a139135ddb7f46f29c8e28e3f8252894244ecb6bb2b74aee6eb9b99f4ef8`. This makes artifact digests, declared authority/state_change, upstream dependencies, and blockers inspectable from one JSON/Markdown surface while keeping the index separate from permission, truth, memory, source registration, runtime authority, and canonical graph semantics.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_OPERATOR_EVIDENCE_INTAKE_REPRODUCIBILITY_CHECK_SLICE_24` is consumed. `experiments/epistemic_readiness/` now has a deterministic advisory checker comparing checked-in operator evidence intake artifacts with freshly regenerated output from the checked-in manifest. Real output: `reproducibility_status=reproducible`, `recommended_human_decision=none`, `action_readiness=advisory_report_allowed`, `blocker_count=0`, `mismatch_count=0`, `digest_match=true`, and regenerated/checked digest `c64e3cde2a68c7aa596d7467ec5e993ac76deda1a8ee83ab83fde1ae23d48e70`. This proves the checked-in intake report is current, while explicitly keeping reproducibility separate from permission and truth.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_OPERATOR_EVIDENCE_INTAKE_STRESS_SLICE_23` is consumed. `experiments/epistemic_readiness/` now has a deterministic advisory stress matrix over manifest-driven operator evidence intake. Real output: `8` scenarios, `8` pass, `0` fail, `all_scenarios_passed=true`, `blocker_count=7`, `boundary_error_count=7`. Decisions are: `clean_manifest=none/advisory_report_allowed`; `missing_artifact`, `stale_digest`, `root_escape`, `non_json_artifact`, `mutating_payload`, `duplicate_artifact_id`, and `missing_required_artifact` all become `review_blockers/blocked` with visible errors. This hardens declared-evidence intake under degraded manifests without making it permission or truth.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_OPERATOR_EVIDENCE_INTAKE_MANIFEST_SLICE_22` is consumed. `experiments/epistemic_readiness/` now has a deterministic manifest-driven advisory intake layer over declared operator evidence artifacts. Real output: `recommended_human_decision=none`, `action_readiness=advisory_report_allowed`, `blocked=false`, `blocker_count=0`, `input_count=6`, `source_artifact_count=4`, packet `none/no_action`, stress matrix `6` scenarios / `6` pass / `0` fail, and all six declared digests match their expected values. This makes the portable operator bundle reproducible from a manifest while keeping digest equality as evidence only, not permission or truth.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_OPERATOR_EVIDENCE_BUNDLE_STRESS_SLICE_21` is consumed. `experiments/epistemic_readiness/` now has a deterministic advisory stress matrix over the operator evidence bundle. Real output: `7` scenarios, `7` pass, `0` fail, `all_scenarios_passed=true`, `blocker_count=6`, and `boundary_error_count=6`. Decisions are: `clean_bundle=none/no_action`; `missing_operator_packet`, `mutating_operator_packet`, `malformed_stress_matrix`, `mutating_source_artifact`, `duplicate_input_id`, and `digest_summary_mismatch` all become `review_blockers/blocked` with visible errors. This hardens the portable handoff bundle against degraded evidence without making it permission.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_OPERATOR_EVIDENCE_BUNDLE_SLICE_20` is consumed. `experiments/epistemic_readiness/` now has a deterministic advisory operator evidence bundle that packages the operator decision packet, operator packet stress matrix, and source advisory artifact digests into one operator-facing handoff artifact. Real output: `packet_recommended_human_decision=none`, `packet_action_readiness=no_action`, `packet_conformance_passed=true`, `6` stress scenarios, `6` pass, `0` fail, `boundary_error_count=1`, `input_count=6`, and `source_artifact_count=4`. It makes the current decision surface portable, but bundle output and digests are still not permission.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_OPERATOR_PACKET_STRESS_MATRIX_SLICE_19` is consumed. `experiments/epistemic_readiness/` now has a deterministic advisory stress matrix over the operator decision packet. Real output: `6` scenarios, `6` pass, `0` fail, `all_scenarios_passed=true`. Decisions are: `clean_no_action=none/no_action`, `handoff_human_review=provide_missing_evidence/human_approval_required`, `conformance_failure=review_blockers/blocked`, `drift_review_required=approve_baseline_refresh/human_approval_required`, `lifecycle_blocker=review_blockers/blocked`, and `malformed_boundary=review_blockers/blocked` with the boundary error visible. This hardens the packet under degraded evidence without making it permission.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_OPERATOR_DECISION_PACKET_SLICE_18` is consumed. `experiments/epistemic_readiness/` now has a deterministic advisory operator decision packet that combines metacognitive handoff, decision-taxonomy conformance, drift policy, and baseline lifecycle evidence into one operator-facing summary. Real output: `recommended_human_decision=none`, `action_readiness=no_action`, `conformance_passed=true`, `18` sources, `35` candidates, `35` findings, `35` ready, `0` blocked, `0` insufficient, and no missing evidence. It makes the current no-action posture easy to inspect, but packet output is still not permission.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_DECISION_TAXONOMY_CONFORMANCE_SLICE_17` is consumed. `experiments/epistemic_readiness/` now has a deterministic advisory conformance report that checks the real stress-matrix decision/readiness pairs against the human-decision taxonomy. Real output: `5` cases, `5` pass, `0` fail, `all_cases_passed=true`; covered pairs are `none/no_action`, `provide_missing_evidence/human_approval_required`, `adjudicate_conflict/human_approval_required`, `approve_baseline_refresh/human_approval_required`, and `review_blockers/blocked`. Incompatible pairs are surfaced as failures; conformance pass is still not permission.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_HUMAN_DECISION_TAXONOMY_SLICE_16` is consumed. `experiments/epistemic_readiness/` now has a deterministic advisory taxonomy for handoff human decisions. Real output: `6` decisions in stable order (`none`, `acknowledge`, `approve_baseline_refresh`, `adjudicate_conflict`, `provide_missing_evidence`, `review_blockers`), all with `can_mutate_state=false` and `can_grant_permission=false`. Compatibility is explicit: `none -> no_action`, `acknowledge -> advisory_report_allowed|observe_only`, `approve_baseline_refresh|adjudicate_conflict|provide_missing_evidence -> human_approval_required`, and `review_blockers -> blocked`. Incompatible pairs are surfaced as issues; compatible pairs are still not permission.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_HANDOFF_STRESS_MATRIX_SLICE_15` is consumed. `experiments/epistemic_readiness/` now has a deterministic advisory stress matrix over the metacognitive handoff. Real output: `5` scenarios, `5` pass, `0` fail. Decisions are: `clean_no_action=none/no_action`, `insufficient_evidence=provide_missing_evidence/human_approval_required`, `active_conflict=adjudicate_conflict/human_approval_required`, `drift_review_required=approve_baseline_refresh/human_approval_required`, and `protocol_blocker=review_blockers/blocked`. This hardens the handoff shape under degraded evidence without making it permission, memory, authority, runtime gate, canonical state, claim graph, automatic learning, or negative evidence from silence.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_METACOGNITIVE_HANDOFF_SLICE_14` is consumed. `experiments/epistemic_readiness/` now has a deterministic advisory metacognitive handoff layer that consumes readiness trace, baseline lifecycle, protocol self-audit, and drift-policy evidence. Real output: `18` sources, `35` candidates, `35` findings, `35` ready, `0` blocked, `0` insufficient, `known_count=5`, `unknown_count=3`, `conflict_count=0`, `missing_evidence_count=0`, `risk_note_count=4`, `recommended_human_decision=none`, `action_readiness=no_action`, `state_change: none`. The point is not to grant action; it is to make explicit why no human decision is currently required while preserving `silence != negative evidence` and `handoff != permission`.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_BASELINE_REFRESH_SLICE_13` is consumed. The operator accepted the slice 12 `material_refresh_candidate` through explicit human approval (`sem ser conservador, avance`), so the checked-in derived replay baseline was refreshed to the regenerated advisory trace and the replay bundle was rerun. Final output: `18` sources, `35` candidates, `35` findings, `35` ready, `0` blocked, `0` insufficient; lifecycle `baseline_already_current`; drift policy `no_drift` / `no_action`; `required_human_action=none`; `state_change: none`. This is baseline maintenance for derived evidence only, not permission, memory, authority, runtime gate, canonical state, claim graph, or automatic future refresh.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_DRIFT_POLICY_SLICE_12` is consumed. `experiments/epistemic_readiness/` now classifies replay drift through an advisory drift-policy layer after trace diff, protocol self-audit, and baseline lifecycle. The replay bundle writes `CEREBRO_SELF_EPISTEMIC_DRIFT_POLICY.{json,md}` as derived disposition evidence. Current disposition is `material_refresh_candidate`, requiring `approve_baseline_refresh`; no regression or high/blocking self-audit was detected. It is not permission, memory, authority, a runtime gate, a claim graph, baseline refresh, or automatic promotion/demotion.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_BASELINE_REFRESH_SLICE_11` is consumed. The slice applied the human-approved helper-produced refresh candidate from slice 10 to `CEREBRO_SELF_EPISTEMIC_READINESS_TRACE_BASELINE.json`, emitted `CEREBRO_SELF_EPISTEMIC_BASELINE_REFRESH.{json,md}`, and regenerated report, trace, trace diff, protocol self-audit, and lifecycle through the replay-bundle helper. Final helper output: `18` sources, `33` candidates, `33` findings, `33` ready, `0` blocked, `0` insufficient, `state_change: none`; lifecycle `recommendation=baseline_already_current`, `required_human_action=none`, `action_readiness=no_action`, `drift_total=0`, `has_regression=false`, `self_audit_high_or_blocking_count=0`. Validation: `epistemic_readiness` `37/0`, architecture/doc governance `64/0`, full AGENTS-equivalent gate `923/0/0/6`. This is derived replay-baseline maintenance only, not permission, memory, authority, runtime gate, canonical state, claim graph, or automatic future refresh.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_REPLAY_ORCHESTRATOR_SLICE_10` is consumed. `experiments/epistemic_readiness/` now has a replay-bundle helper (`ReplayBundle`, `ReplayBundlePaths`, `ReplayBundleWriteResult`, `build_replay_bundle`, `write_replay_bundle`) that regenerates report, trace, trace diff, protocol self-audit, and baseline lifecycle artifacts in one tested advisory path. It writes only declared derived outputs and rejects `.cerebro/`, root-escape, and checked-in baseline targets. Real helper output: `17` sources, `32` candidates, `32` findings, `32` ready, `0` blocked, `0` insufficient, `baseline_updated=false`, `state_change: none`. Lifecycle now reports `recommendation=refresh_candidate_requires_human_approval`, `required_human_action=approve_baseline_refresh`, `action_readiness=human_approval_required`, `drift_total=74`, `has_regression=false`, `self_audit_high_or_blocking_count=0`. Validation: `epistemic_readiness` `37/0`, architecture/doc governance `64/0`, full AGENTS-equivalent gate `923/0/0/6`. This is replay automation only, not permission, memory, authority, runtime gate, canonical state, claim graph, or automatic baseline refresh.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_BASELINE_REFRESH_SLICE_9` is consumed. The slice applied the human-approved advisory refresh candidate to `CEREBRO_SELF_EPISTEMIC_READINESS_TRACE_BASELINE.json`, emitted `CEREBRO_SELF_EPISTEMIC_BASELINE_REFRESH.{json,md}`, and regenerated the replay bundle. Final lifecycle result: `recommendation=baseline_already_current`, `required_human_action=none`, `action_readiness=no_action`, `drift_total=0`, `has_regression=false`, `state_change: none`. This is derived replay-baseline maintenance only: it does not grant authority, memory, permission, runtime gate, canonical state, or claim graph. Validation: `epistemic_readiness` `33/0`, architecture/doc governance `64/0`, full AGENTS-equivalent gate `923/0/0/6`.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_BASELINE_LIFECYCLE_SLICE_8` is consumed. `experiments/epistemic_readiness/` now emits advisory baseline lifecycle reports (`BaselineLifecycleReport`, `DriftCounts`, `evaluate_baseline_lifecycle`, JSON/Markdown renderers) from trace, trace-diff, and protocol self-audit evidence. Current self-readiness replay has `15` sources, `30` candidates, `30` findings, `0` blocked, `0` insufficient. Real lifecycle result: `recommendation=refresh_candidate_requires_human_approval`, `required_human_action=approve_baseline_refresh`, `action_readiness=human_approval_required`, `drift_total=73`, `has_regression=false`, `self_audit_high_or_blocking_count=0`, `state_change: none`. The checked-in baseline was not overwritten; the output is review evidence only, not authority, memory, permission, runtime gate, or claim graph. Validation: `epistemic_readiness` `33/0`, architecture/doc governance `64/0`, full AGENTS-equivalent gate `923/0/0/6`.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_IDENTITY_STABILITY_SLICE_7` is consumed. `experiments/claim_extraction/` now emits stable `semantic_id` plus traceability-preserving `evidence_id` for each `ClaimCandidate`; `experiments/epistemic_readiness/` carries those ids into decision traces and separates semantic `changed` from `traceability_changed` in trace diffs. Current self-readiness replay has `14` sources, `29` candidates, `29` findings, `0` blocked, `0` insufficient. Real replay result: candidates `3` added, `0` removed, `0` semantically changed, `26` traceability-changed; findings `3` added, `0` removed, `0` semantically changed, `26` traceability-changed; `has_regression=false`; `state_change: none`. Self-audit now reports `evidence_traceability_drift` and `source_surface_drift`, not `evidence_identity_churn`; `0` high/blocking; `action_readiness=advisory_report_allowed`. This is replay hygiene only, not permission, memory, authority, or a claim graph. Validation: claim extraction/evaluation/readiness integration `45/0`, focused derived + architecture/doc governance `109/0`, full AGENTS-equivalent gate `923/0/0/6`.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_PROTOCOL_SELF_AUDIT_SLICE_6` is consumed. `experiments/epistemic_readiness/` now emits advisory protocol self-audit candidates (`ProtocolAuditCandidate`, `ProtocolSelfAuditReport`, `load_trace_diff_json`, `audit_protocol_from_trace_diff`, JSON/Markdown renderers) from trace-diff evidence. Current self-readiness replay has `13` sources, `28` candidates, `28` findings, `0` blocked, `0` insufficient. Generated self-audit artifacts report `2` candidates: `evidence_identity_churn` and `source_surface_drift`; `0` high/blocking; `action_readiness=advisory_report_allowed`; `state_change: none`. This is anti-noise review evidence only: it does not write memory, apply learning, promote/demote authority, mutate state, create a claim graph, or grant permission. Validation: `epistemic_readiness` `27/0`, claim extraction/evaluation/readiness integration `43/0`, architecture/doc governance `64/0`, full AGENTS-equivalent gate `923/0/0/6`.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_TRACE_DIFF_SLICE_5` is consumed. `experiments/epistemic_readiness/` now compares structured advisory decision traces (`TraceDiff`, `load_decision_trace_json`, `compare_decision_traces`, `render_trace_diff_json`, `render_trace_diff_markdown`). The self-readiness replay keeps `CEREBRO_SELF_EPISTEMIC_READINESS_TRACE_BASELINE.json`, regenerates `CEREBRO_SELF_EPISTEMIC_READINESS_TRACE.json`, and emits `CEREBRO_SELF_EPISTEMIC_READINESS_TRACE_DIFF.{json,md}`. Current trace: `12` sources, `27` candidates, `27` findings, `0` blocked, `0` insufficient. Current diff: `1` added source read, `2` changed source reads, `1` added candidate, `26` changed candidates, `1` added finding, `26` changed findings, `0` guardrail changes, `has_regression=false`, `advisory_readiness=no_regression_observed`, `state_change: none`. Candidate/finding identity uses semantic signatures, so line-shift churn is visible as `claim_id`/`evidence_span` change rather than false add/remove churn. Validation: `epistemic_readiness` `24/0`, claim extraction/evaluation/readiness integration `40/0`, architecture/doc governance `64/0`, full AGENTS-equivalent gate `923/0/0/6`. This remains replay/regression evidence only, not permission.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_DECISION_TRACE_SLICE_4` is consumed. `experiments/epistemic_readiness/` now emits structured advisory decision traces (`DecisionTrace`, `build_decision_trace`, `render_decision_trace_json`, `generated_trace`, `resolve_generated_trace_path`) from the same manifest as the Markdown report. `CEREBRO_SELF_EPISTEMIC_READINESS_TRACE.json` records `11` source reads, `26` candidates, `26` findings, `26` ready, `0` blocked, `0` insufficient, `action_readiness=derived_experiment_allowed`, `state_change: none`, and explicit guardrails that report readiness, risk readiness, trace presence, and manifest presence are not permission. Validation: `epistemic_readiness` `20/0`, claim extraction/evaluation/readiness integration `36/0`, architecture/doc governance `64/0`, full AGENTS-equivalent gate `923/0/0/6`. This remains replay/self-audit evidence only, not permission.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_ACTION_MANIFEST_SLICE_3` is consumed. `experiments/epistemic_readiness/` now loads checked-in TOML manifests (`ReadinessManifest`, `load_readiness_manifest`, `generate_readiness_report_from_manifest`, `resolve_generated_report_path`) and the self-readiness manifest declares a bounded action proposal for regenerating the advisory report. The slice 3 closure report read `10` bounded sources and reported `25` candidates, `25` findings, `25` ready, `0` blocked, `0` insufficient, `action_readiness=derived_experiment_allowed`, plus risk evidence `risk_score=1`, `required_gate_level=G2`, `budget_status=within_budget`, `human_approval_required=false`, `state_change: none`; slice 4 later reran the report with generated trace support. Validation: `epistemic_readiness` `17/0`, claim extraction/evaluation/readiness integration `33/0`, architecture/doc governance `64/0`, full AGENTS-equivalent gate `923/0/0/6`. This remains advisory evidence only, not permission.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_RISK_BUDGET_SLICE_2` is consumed. `experiments/epistemic_readiness/` now evaluates declared action risk through `ActionProposal`, `BlastRadiusDeclaration`, `RiskBudget`, `RiskAssessment`, and `evaluate_risk_budget(...)`; reports can render a `Risk Budget Assessment` with required gate, action readiness, budget violations, human approval requirement, stop conditions, and `state_change: none`. Validation: `epistemic_readiness` `14/0`, claim extraction/evaluation/readiness integration `30/0`, architecture/doc governance `64/0`, full AGENTS-equivalent gate `923/0/0/6`. This remains advisory evidence only, not permission.
- Gate correction closed on 2026-04-24: `FORMAL_RESUME_TRIGGER_GATE_FLAKE_SESSION_REFRESH_TIMESTAMP` is consumed. A flaky `test_state_store` assertion compared raw `state.json` bytes across `validate_state()` even though `last_validation.validated_at` legitimately changes. The test now forces a changed timestamp, normalizes only that field, and still proves the crash-recovery invariants. Validation: affected test `1/0`, 30-iteration loop `30/0`, `tests.test_state_store` `93/0`, full gate `923/0/0/6`.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_SELF_REPORT_RERUN` is consumed. `docs/operations/CEREBRO_SELF_EPISTEMIC_READINESS_REPORT.md` is now generated by `experiments/epistemic_readiness` from `CEREBRO_SELF_EPISTEMIC_READINESS_MANIFEST.toml`, with `21` candidates, `21` findings, `21` ready, `0` blocked, `0` insufficient, and `action_readiness=advisory_report_allowed`. This closes the one-off report gap while preserving `state_change: none`, advisory-only posture, no runtime gate, no claim graph, and no authority promotion.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_READINESS_REPORT_GENERATOR_SLICE_1` is consumed. New derived package `experiments/epistemic_readiness/` generates advisory readiness reports from explicit source manifests, bounded source heads, `claim_extraction`, and `claim_evaluation`; it renders stable Markdown with `state_change: none`, baseline comparison hooks, action-readiness, and explicit report-is-not-permission guardrails. Focused tests pass `7/0`; extraction/evaluation integration remains `16/0`; no runtime gate, claim graph, state mutation, or authority promotion was introduced.
- Risk-Adaptive Epistemic Runtime spec hardened on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_AUTHORITY_RUNTIME_SPEC_HARDENING` is consumed. `docs/operations/EPISTEMIC_AUTHORITY_RUNTIME_SPEC.md` is now a 25-section operational specification covering scope, authority states, zones, reversibility-weighted authorization, blast radius, risk budget, gate ladder, promotion/demotion, confidence propagation, sufficiency, memory decay, staleness taxonomy, action readiness, metacognitive handoff, epistemic observability, human approvals, anti-noise, standing authorization for derived experiments, protocol self-audit, recovery/re-promotion, examples, stop conditions, and explicit non-authorization. It is still policy/spec only and authorizes no runtime implementation.
- Risk-Adaptive Epistemic Runtime matured into a docs-only authority spec on 2026-04-24: `FORMAL_RESUME_TRIGGER_EPISTEMIC_AUTHORITY_RUNTIME_SPEC` is consumed. `docs/operations/EPISTEMIC_AUTHORITY_RUNTIME_SPEC.md` defines operational zones, reversibility-weighted authorization, blast radius declaration, authority states, promotion/demotion, confidence propagation, protocol self-audit, and the permission-vs-evidence split. It is policy/spec only and authorizes no runtime implementation.
- Epistemic-runtime self-readiness rerun closed on 2026-04-24: `FORMAL_RESUME_TRIGGER_CEREBRO_SELF_EPISTEMIC_READINESS_RERUN` is consumed. The normalized report now shows `17` candidates, `17` findings, `17` ready, `0` blocked, `0` insufficient, and confirms temporal trigger subjects are clean trigger ids rather than long snapshot bullets. The advisory conclusion did not change.
- Epistemic-runtime lane closed the first extraction-quality fix on 2026-04-24: `FORMAL_RESUME_TRIGGER_CLAIM_EXTRACTION_TEMPORAL_NORMALIZATION` is consumed. `claim_extraction` now normalizes inline trigger-consumption claims to the trigger id rather than full snapshot bullets; focused extraction tests are `9/0`, evaluation remains `7/0`, and the layer is still advisory-only.
- Epistemic-runtime lane produced its first self-management readiness report on 2026-04-24: `FORMAL_RESUME_TRIGGER_CEREBRO_SELF_EPISTEMIC_READINESS` is consumed. `docs/operations/CEREBRO_SELF_EPISTEMIC_READINESS_REPORT.md` evaluated bounded heads of Cerebro's own operational sources through `claim_extraction` + `claim_evaluation`: `15` candidates, `15` findings, `15` ready, `0` blocked, `0` insufficient, `state_change: none`. The report says the derived epistemic-runtime lane can continue, and it identifies the next concrete improvement: normalize temporal trigger-consumption claim subjects.
- Epistemic-runtime lane advanced again on 2026-04-24: `FORMAL_RESUME_TRIGGER_CLAIM_EVALUATION_SLICE_1` is consumed. The new `experiments/claim_evaluation/` track evaluates `ClaimCandidate` inputs with bounded advisory findings for authority, confidence, sufficiency, conflict, supersession, staleness-by-conflict, and operational readiness. Focused tests passed `7/0`; extraction remains `8/0`; the evaluator is derived/read-only/non-authoritative and does not create a claim graph or runtime gate.
- Epistemic-runtime lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_CLAIM_EXTRACTION_SLICE_1` is consumed. The new `experiments/claim_extraction/` track implements deterministic `ClaimCandidate` extraction from bounded text, with 12 executable fixtures covering silence, structured absence, supersession absence, citation authority, meta-claims, temporal status, criticality hints, over-extraction, future-trigger drafts, and the full Fixture 9 schema-omission oracle. Focused tests passed `8/0`; the layer remains derived/read-only/non-authoritative and does not create a claim graph.
- New-tech lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_CONTEXT_ADVISOR_LLM_SLICE_1` is consumed. `experiments/context_advisor/` now emits an LLM-facing advisory report that combines `context_discovery` candidate/drift/missing-source evidence with `context_vectors` ranking evidence. It is local-only, read-only, non-authoritative, and explicitly says `may_suggest` / `must_not_apply`.
- New-tech lane synthesized on 2026-04-24: `FORMAL_RESUME_TRIGGER_CONTEXT_VECTORS_ORACLE_SYNTHESIS` is consumed. Four real oracles now provide the maturity baseline for `context_vectors`: `20` total cases, `15/20` top-1, `20/20` top-3, aggregate `recall_at_1=0.750`, aggregate `recall_at_3=1.000`. Decision: build an LLM-facing advisory report next; do not promote retrieval into runtime authority or automatic import.
- New-tech lane stress-tested again on 2026-04-24: `FORMAL_RESUME_TRIGGER_CONTEXT_VECTORS_ESCRITORIO_EVAL` is consumed. The Escritorio IRPF/Caixa Rural oracle exposed `max_files` starvation from unsupported binary/XML/PDF noise and deep-artifact dominance over shallow system/index READMEs; the indexer now counts only indexable text candidates toward `max_files` and lifts shallow general surfaces for system/registry queries. Final report passed with `recall_at_1=1.000`, `recall_at_3=1.000`, `all_cases_passed_at_3=true`, and no target mutation.
- New-tech lane stress-tested on 2026-04-24: `FORMAL_RESUME_TRIGGER_CONTEXT_VECTORS_PORTAL_EVAL` is consumed. The dense Portal Humaita oracle initially exposed historical/temp/nested-Cerebro noise; the indexer now skips archival/temp/nested-Cerebro directories and adds live-surface metadata cues. Final Portal report passed top-3 with `recall_at_1=0.500`, `recall_at_3=1.000`, `critical_continuity_result=pass`, and no target mutation.
- New-tech lane advanced on 2026-04-24: `FORMAL_RESUME_TRIGGER_CONTEXT_VECTORS_SLICE_1` is consumed, and `experiments/context_vectors/` now provides deterministic local vector search over bounded textual heads with hidden-file skipping, ranked hits, trace metadata, and eval scoring. It is derived/read-only/non-authoritative and does not change `.cerebro/` or target projects.
- New-tech lane advanced again on 2026-04-24: `FORMAL_RESUME_TRIGGER_CONTEXT_VECTORS_ORACLE_EVAL` is consumed. The real `rpg_caminhada` oracle first exposed a pure-vector miss (`04_MEMORIA_CONTINUIDADE_ATUAL.md` ranked `4`), then bounded path/heading metadata cues moved the critical continuity file to rank `1`; final report is `recall_at_1=1.000`, `recall_at_3=1.000`, `critical_continuity_result=pass`.
- New-tech lane cross-validated on 2026-04-24: `FORMAL_RESUME_TRIGGER_CONTEXT_VECTORS_CEREBRO_SELF_EVAL` is consumed. The Cerebro self-oracle exposed `_local/legacy` archive dominance, `_local/` is now skipped, and final self-eval passed top-3 with `recall_at_1=0.500`, `recall_at_3=1.000`, `all_cases_passed_at_3=true`.
- Third-party project pilot preparation is historical: the prepared trigger, runbook, Claude recon, intake gate, and recommendation were consumed by the live `rpg_caminhada` pilot execution on 2026-04-24.
- Formal resume trigger consumed on 2026-04-23: `FORMAL_RESUME_TRIGGER_CONTEXT_DISCOVERY_EXT_SLICE_1` — derived track `experiments/context_discovery/` is canonical non-authoritative; full AGENTS-equivalent gate green at `923/0/0/6` on Windows; predecessor trigger `FORMAL_RESUME_TRIGGER_BOOTSTRAP_SCAN_ENRICHMENT` withdrawn the same day for architecture-invariant collision and reverted to HEAD; layering rule captured in `docs/handoffs/HANDOFF_CONTENT_AWARE_ANALYSIS_LAYERING.md`.
- Canonical gate after docs-only reconciliation: `958` tests, `0` failures, `0` errors, `6` skips measured with the AGENTS-equivalent runner (2026-04-29, Windows).
- Architecture gate: `51` tests, `0` failures.
- Derived tracks validation: `recall_eval` `49/0`, `operational_signals` base `31/0` and `suggestions` `97/0`, `context_discovery` `12/0`, `context_vectors` `21/0`, `context_advisor` `6/0`, `claim_extraction` `9/0`, `claim_evaluation` `7/0`, `epistemic_readiness` `145/0` focused for slice 33, `epistemic_guard` `33/0`.
- Current posture: `runtime-manager-phase-1` is open under `FORMAL_RESUME_TRIGGER_RUNTIME_MANAGER_PHASE_1.md`. The core store and CLI surface now exist: `runtime-manager sync/status/next` calls `core.runtime_manager_store`, explains ready/stale/no-op states, gates required decisions/evidence/tools/approvals/validations/stop conditions from structured tables, exposes `gate_diagnostics` and `selection_audit`, preserves append-only events, honors active single-flight leases, counts replay runs, writes projection-only `--out` files, and keeps execution out of scope. Phase 1 remains scheduler/gates/status first, not execution.
- Allowed non-growth lanes under freeze: compacting or disambiguating operational docs only when they are oversized, duplicated, or live-sounding (slice 2b.1 consolidated this document and `SYSTEM_STATE.md`; slice 2b.3 marked `BUG_REPORT.md` and `PROJECT_OS_BACKLOG.md` as historical ledgers; both closed with a green full gate), local visual organization that preserves ignored/generated/legacy artifacts under `_local/` without deletion (closed on 2026-04-24), and the planning-only `StateStore` decomposition prep in `docs/operations/STATESTORE_DECOMPOSITION_PLAN.md` (dormant).
- Local visual organization: repo-root temp/cache/sandbox artifacts and old Cerebro-specific parent-level backups were moved to `_local/root_cleanup_2026-04-24/`; active project surfaces and sibling projects were left in place.
- Authority order: `AGENTS.md -> active triggers -> observation_center.toml -> SYSTEM_STATE.md -> OPPORTUNITY_MAP.md -> active plans -> code/tests`. Divergence forces docs-only reconciliation before implementation.
- Queue surface: `observation_center.toml` is the machine-primary queue import source for this bridge; `.cerebro/runtime.db` is the core read model plus decision/evidence/tool/approval/event/replay/lease/validation/stop-condition ledgers, gate diagnostics, and selection audit; this map and CLI output files are projections, not the scheduler.
- The canonical `SCOUT_CONTROL_STATE` lives only in `SYSTEM_STATE.md`; this map carries only the minimal next-action projection for heartbeat routing.
- Next item: add runtime-manager lease expiry/readiness semantics behind core APIs so expired active leases cannot permanently distort single-flight selection. No adapter, target work, cloud work, source import, or free execution is routed by this opening.

The snapshot above is the only live operational reference. Everything below is historical evidence preserved for audit, including the `DOC-001`–`DOC-005` documentary lineage (status `done`), the blocked-by-freeze set, and the derived-fix chronology.

## NEXT_ACTION

```text
NEXT_ACTION
- next_required_step: implement_authorized_observation_center_sqlite_promotion
- observation_center_head: observation-center-sqlite-ledger is open under FORMAL_RESUME_TRIGGER_OBSERVATION_CENTER_SQLITE_LEDGER_2026-05-23
- active_renewal_debt: none while the canonical gate stays green
- highest_priority_hypothesis: the TOML center has served as a bootstrap bridge; the next value is making runtime.db the local primary authority for observations and dependencies while keeping TOML export/import compatibility and avoiding any external scheduler or execution loop.
```

## Historical Documentary Queue

### DOC-001 — Reconcile Documentary Drift

- Status: `done`
- Priority: `HIGH`
- Level: `1`
- Why it exists:
  - `BUG_REPORT.md` says the remaining corrective backlog is "dois residuos explicitos" but does not enumerate both, leaving residual intake ambiguous
  - `PHASE_CLOSURE.md` still closes the prior phase at `534` tests even though the current suite is `548`
  - `AGENTS.md` still shows a Unix-style `tail -5` snippet even though this repo runs under PowerShell
- Scope:
  - `docs/operations/BUG_REPORT.md`
  - `docs/operations/PHASE_CLOSURE.md`
  - `AGENTS.md`
- Done when:
  - residuals are explicit instead of implicit
  - the current documentary closure matches the post-Round-10 test count and status
  - the preflight suite command is portable to the declared shell
- Verify:
  - `python -m unittest discover -s tests -v`
- Result:
  - `BUG_REPORT.md` now names the accepted residuals explicitly
  - `PHASE_CLOSURE.md` now reflects the post-Round-10 `548`-test closure state
  - `AGENTS.md` now uses a PowerShell-portable preflight snippet

### DOC-002 — Proof Of Stop And Formal Re-Closure

- Status: `done`
- Priority: `MEDIUM`
- Level: `1`
- Depends on: `DOC-001`
- Why it exists:
  - after documentary reconciliation, the loop still needs an explicit stop-proof pass and refreshed closure record for the current frozen state
- Scope:
  - `docs/operations/PHASE_CLOSURE.md`
  - `docs/operations/SYSTEM_STATE.md`
  - `docs/operations/OPPORTUNITY_MAP.md`
- Done when:
  - the queue distinguishes executable items from blocked items cleanly
  - accepted residuals are named explicitly
  - the phase closure reflects the current green suite and current blocked items
- Verify:
  - `python -m unittest discover -s tests -v`
  - `python -m unittest tests.test_architecture -v`
- Result:
  - `P1`: no new eligible performance hotspot
  - `P2`: no new bypass surface beyond accepted residuals and freeze-blocked items
  - `P3`: no new recovery/failure scenario outside the accepted residual set
  - one subagent reported a red suite locally, but the focal rerun of `tests.test_state_store.StateStoreTests.test_open_session_restores_registry_and_external_artifacts_when_session_file_write_fails` passed in the main workspace
  - `P5` gap closed: `PHASE_CLOSURE.md` now sits inside the automated documentary proof perimeter through explicit guards in `tests/test_doc_governance.py` and `tests/test_architecture.py`
  - formal re-closure is no longer blocked by the proof-perimeter gap

### DOC-003 — Reconcile Helper Alias Drift

- Status: `done`
- Priority: `LOW`
- Level: `1`
- Depends on: `DOC-002`
- Why it exists:
  - the proof-of-stop `P4` pass found that `AGENTS.md` used helper label `architect` as if it were a canonical role, which drifted from the seven-role canonical set
  - the live documentary gate had moved to `550` tests after proof-hardening, while the active map/state snapshot still showed `548`
- Scope:
  - `AGENTS.md`
  - `docs/operations/SYSTEM_STATE.md`
  - `docs/operations/OPPORTUNITY_MAP.md`
  - `docs/operations/PHASE_CLOSURE.md`
- Done when:
  - helper aliases are explicitly non-canonical
  - the documenter-only track boundary is explicit in the repo operating note
  - the live documentary gate is refreshed to `550` tests without rewriting the historical closure count inside `PHASE_CLOSURE.md`
- Verify:
  - `python -m unittest tests.test_doc_governance tests.test_architecture -v`
  - `python -m unittest discover -s tests -v`
- Result:
  - helper aliases are now explicit non-canonical tooling labels in `AGENTS.md`
  - the documenter-only track boundary is explicit again in the repo operating note
  - the live documentary gate in `OPPORTUNITY_MAP.md` and `SYSTEM_STATE.md` now reflects the `550`-test suite state
  - `PHASE_CLOSURE.md` now states that the `548`-test count remains the historical closure count, while later documentary proof-hardening lifted only the live suite gate

### DOC-004 — Reconcile New Weakness Intake From The 2026-04-17 Audit

- Status: `done`
- Priority: `HIGH`
- Level: `1`
- Depends on: `DOC-003`
- Why it exists:
  - the direct 2026-04-17 audit reproduced a policy gap where `fs.create_file` with `overwrite=true` still mutates an existing file without approval because the gate is keyed by `kind`
  - the same audit reproduced a rollback residual where `create-new` removes the file but leaves an empty directory
  - `BUG_REPORT.md`, `WEAKNESS_REPORT.md`, and `SYSTEM_STATE.md` did not yet reflect those findings, so the canonical queue understated the live blocked set
- Scope:
  - `docs/operations/WEAKNESS_REPORT.md`
  - `docs/operations/BUG_REPORT.md`
  - `docs/operations/OPPORTUNITY_MAP.md`
  - `docs/operations/SYSTEM_STATE.md`
- Done when:
  - the effect-level approval gap is explicitly named as open
  - the rollback empty-directory residual is explicitly named as open
  - the blocked set and weakness intake reflect the current confirmed queue instead of the pre-audit queue
- Verify:
  - `python -m unittest discover -s tests -v`
  - `python -m unittest tests.test_architecture -v`
- Result:
  - the live weakness queue now includes the new effect-level approval gap and the rollback empty-directory residual
  - `BUG_REPORT.md` no longer claims that no approval residual remains open
  - `OPPORTUNITY_MAP.md` and `SYSTEM_STATE.md` now expose the fuller blocked set for the current documentary freeze track

### DOC-005 — Harden Preflight Gate Summary Against Noisy Test Output

- Status: `done`
- Priority: `MEDIUM`
- Level: `1`
- Depends on: `DOC-004`
- Why it exists:
  - the mandatory preflight snippet in `AGENTS.md` still relied on `Select-Object -Last 5`
  - in this workspace, `python -m unittest discover -s tests -v` can emit trailing operational lines after the suite summary, so the tail can hide the actual verdict
- Scope:
  - `AGENTS.md`
  - `docs/operations/OPPORTUNITY_MAP.md`
  - `docs/operations/SYSTEM_STATE.md`
- Done when:
  - the preflight command still runs in PowerShell
  - the preflight surfaces a stable suite verdict even when test output is noisy
  - the documentary snapshot records why the old tail-based check was insufficient
- Verify:
  - `python -m unittest discover -s tests -v`
  - `python -m unittest tests.test_architecture -v`
- Result:
  - `AGENTS.md` now captures the suite output in a temporary file and prints the final `Ran ... tests` line plus a stable pass/fail summary instead of trusting the raw tail
  - the documentary state now records that the previous tail-based preflight could hide the true verdict in this workspace

## Blocked By Freeze Or Architecture

### BLOCKED-102 — External session ownership residual

- Source: `docs/operations/PROJECT_OS_BACKLOG.md`
- Status: `blocked`
- Reason: touches session hardening beyond the current frozen corrective envelope

### BLOCKED-103 — Verify isolation residual

- Source: `docs/operations/PROJECT_OS_BACKLOG.md`
- Status: `blocked`
- Reason: touches residual verify authority/isolation boundaries that are accepted but not currently approved for growth

### BLOCKED-105 — Apply/Rollback external-writer atomicity

- Source: `docs/operations/PROJECT_OS_BACKLOG.md`
- Status: `blocked`
- Reason: would require deeper runtime hardening on critical execution paths

### BLOCKED-106 — Project/protocol anchoring

- Source: `docs/operations/PROJECT_OS_BACKLOG.md`
- Status: `blocked`
- Reason: remains procedural and would require architecture-level authorization before canonical promotion

### BLOCKED-GAP-02 — Canonical intention entity

- Source: `docs/operations/adr/GAP-02-intencao-canonica.md`
- Status: `blocked`
- Reason: explicit ADR block under freeze; no schema/state expansion is authorized

### BLOCKED-ALIGNMENT-EXPORT

- Source: `docs/operations/FREEZE_POLICY.md`
- Status: `blocked`
- Reason: no canonical alignment artifact exists

### BLOCKED-P5-STRUCTURE — `PHASE_CLOSURE.md` structural proof hardening

- Source: proof-of-stop `P5`
- Status: `blocked`
- Reason: targeted literal guards already cover the current closure claim; deeper structural hardening would require a separate test-surface slice outside the active documenter-only queue

### BLOCKED-WEAK-CRIT-LEGACY-EXEC — `exec.command` post-mutation artifact persistence gap

- Source: `docs/operations/WEAKNESS_REPORT.md`
- Status: `blocked in documenter track`
- Reason: the confirmed critical fix requires mutating `core/action_runtime.py`, `cli/commands/apply.py`, and proportional regression tests, which this loop may not touch under `AGENTS.md`

### BLOCKED-WEAK-HIGH-001 — session refresh crash window

- Source: `docs/operations/WEAKNESS_REPORT.md`
- Status: `blocked in documenter track`
- Reason: the confirmed high-severity fix requires mutating `core/state_store.py` and proportional regression tests, which this loop may not touch under `AGENTS.md`

### BLOCKED-WEAK-HIGH-002 — open-session crash split

- Source: `docs/operations/WEAKNESS_REPORT.md`
- Status: `blocked in documenter track`
- Reason: the confirmed high-severity fix requires mutating `core/state_store.py` and proportional regression tests to close the `session_registry_mismatch` window, which this loop may not touch under `AGENTS.md`

### CLOSED-WEAK-MED-004 — rollback agora poda tree recém-criada no caso `create-new`

- Source: `docs/operations/WEAKNESS_REPORT.md`
- Status: `closed (recorded 2026-04-22)`
- Resolution: `_prune_empty_workspace_dirs` em `core/action_runtime.py` já poda `created_target_dirs` no `rollback` de `fs.create_file` (linha 1025) e de `fs.move` sem `target_preimage_ref` (linha 1066); regressão em `tests/test_alpha_runtime.py` confirma que `notes/archive/` e `notes/` saem ausentes após rollback de `notes/archive/draft.txt`.

### CLOSED-DOC-DRIFT-002 — `AGENT_ARCHITECTURE.md` realinhado com o contrato do runtime

- Source: `docs/operations/WEAKNESS_REPORT.md`
- Status: `closed 2026-04-22`
- Resolution: `## Canonical Flow` → `## Operational Flow (Agent Protocol Only)` com disclaimer explícito; `## Canonical Roles` → `## Canonical Roles (External Labels Only)` com nota de ausência de role scheduler no runtime; `test_tracked_root_surface_is_minimal_and_docs_are_grouped` atualizado para `.codex` e `AGENTS.md`; 51 testes arquiteturais verdes.

## Accepted Residuals That Are Not Queue Items By Default

- file-backed session ownership does not yet close same-user tamper/restore of the external authority files
- verify still has residual bypass limits for perfectly restored transient tamper, out-of-root effects, or fully concealed drift
- apply/rollback still do not claim perfect atomicity against arbitrary external writers during execution
- performance has no currently eligible hotspot; the remaining event-log scaling risk is future-facing and measurement-gated

## Historical Next Item

- `none — hardening do Grupo 6 concluído`
- Historical closure note preserved for the documentary perimeter: `documenter queue exhausted; await Formal Resume Trigger`
- blocked residuals:
  - deeper architectural residuals still require mutating runtime code and proportional regression coverage
  - `PHASE_CLOSURE.md` structural proof hardening remains outside the active documenter-only queue
- latest revalidation:
  - the weakness queue was re-read and reconciled on 2026-04-17
  - `WEAKNESS_REPORT.md` now exposes `0` `CRÍTICO` aberto, `0` `ALTO` aberto, and multiple `MÉDIO`
  - the deep audit gap on effect-level approval (`fs.create_file overwrite=true` without approval) is now closed; the rollback residual (`create-new` leaves empty directory residue) remains tracked as `MÉDIO`
  - the final Grupo 6 proof pass ended clean: `P1` reproduced no original debt, `P2` found no new regression, and the live gate stayed green at `700` tests plus `51` architecture tests
  - the deep audit also preserved newer `MÉDIO` items (`close_session()` crash split) plus a stronger coverage gap (`session + plan + apply + verify + rollback` end-to-end still fragmented)
  - undocumented runtime behaviors (`plan_generation_id` fallback, auto-filled `consolidation_id`) were added to `WEAKNESS_REPORT.md`
  - gate revalidations (`550` tests, `0` failures, `6` skips; `tests.test_architecture` green) confirmed blocked set yields no executable documentary-only slice
