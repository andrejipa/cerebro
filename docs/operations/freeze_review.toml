# Freeze Review Cadence
#
# Canonical derived artifact tracking freeze posture review state.
# This is NOT part of core state authority: it is an operational
# discipline artifact, consumed by documenter/reviewer flows and
# by `tests/test_doc_governance.py`. It MUST NOT be read or written
# by `core/` or by any runtime mutator.
#
# Dates are absolute ISO-8601 (YYYY-MM-DD). Counts are non-negative
# integers. See `docs/operations/FREEZE_POLICY.md#review-cadence`
# for the cadence contract and `last_outcome.verdict` vocabulary.

[review]
last_review_date = "2026-04-19"
trigger_count_since_review = 0
round_count_since_review = 0
mandatory_review_after_rounds = 50
mandatory_review_after_days = 90

[last_outcome]
date = "2026-04-19"
verdict = "freeze_confirmed"
rationale = "auditoria pós-hardening concluída; prova de parada P1-P5 limpa; nenhum Formal Resume Trigger observado; suíte 704/0/6 estável"
next_review_due = "2026-07-18"
