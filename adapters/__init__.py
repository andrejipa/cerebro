"""Adapter layer -- pilot local adapters that wrap RuntimeManagerStore public APIs.

No MCP server, no network, no external SDK. Each adapter sub-package is an
incubation surface before any real MCP / Agents SDK integration is authorised.
"""
